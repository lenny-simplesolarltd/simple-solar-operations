# S01 manual setup and G01 execution

Current external state: **BLOCKED — INPUT REQUIRED**. These are instructions, not completed actions. Ben supplies access and company identities; the company administrator provisions accounts/resources; the named developer configures the new test projects. No need to share a password or change existing production systems.

1. **Company ownership.** Ben identifies company repository organisation, his company login, administrator, automation owner, recovery owner/contact, developer account and backups. Administrator creates a normally licensed company automation user with MFA and a company recovery path, without production data/calendar access. Use distinct DEV/TEST project connections and protected credentials. Record grants, expiry/review, renewal owner and recovery evidence in the ownership register. No developer super-admin/banking access.
2. **Repository and storage.** Create a private source repository under that company organisation; grant Ben durable owner/access and the named developer contributor access. Import these source files, preserving the four reference documents and reviewing for secrets/production identifiers. Record the initial company commit/tag in the test register. Do not use a personal developer remote as the company repository. Create company project/inventory storage and separate DEV and TEST evidence folders. Use a Shared Drive where the subscribed product/ownership model supports it; otherwise record and verify the company-owned alternative and recovery procedure. Ben must open source and inventory in his own signed-in session.
3. **Isolated resources.** Create new blank DEV and TEST Sheets, separate secondary calendars, and separate Apps Script projects under company control. Name each visibly with its environment. Set business timezone Europe/London in Sheets, calendars and Script manifest; verify AppSheet date presentation later with the actual account/device. Record exact resource IDs privately and compare DEV/TEST/PROD inventories to prove no overlap. Create no business tables yet. Only synthetic setup data is permitted.
4. **AppSheet shell and entitlements.** Create separate DEV/TEST app shells under the company app owner, using only the corresponding synthetic setup Sheet if a source is required to create the app. Require sign-in; share only with named test users. Inspect every table, file source, connection, bot/webhook and inherited permission: none may point at PROD or invoke live automation. Do not copy a production app with its connections/bots. Have one internal and actual external installer/scaffolder test account sign in; record actual plan/partner entitlement and permitted test-source access. This is S01 environment verification, not S04 role-specific workflow/UI implementation. Do not rely on a slice or hidden menu as security. Record unsupported capabilities and agree a path with Ben/developer before depending on them.
5. **Inboxes and access.** Ben approves direct controlled test inboxes and users. Administrator verifies mailbox ownership and absence of forwarding, groups, auto-responses, Calendar sharing/notification rules or integrations that can route tests to real recipients. Connection owner must have no production calendar access; test calendars must not be primary calendars and must be owned by that company test account. Record exact allowlisted recipients and calendar IDs in protected DEV/TEST config. Do not permit wildcard domains, display names or guessed contacts. Test users receive no project-source/config editing rights.
6. **Inventory and backups.** With existing owners, expand every inventory placeholder into actual producers. Export current settings read-only; preserve protected originals, checksum, time and owner. Record actual live state and retirement/retention decisions without executing them. Inventory does not require submitting the live Jotforms or modifying any production workflow. Capture plan/connector operation constraints; actual Xero workflow implementation waits for S13 and test finance facilities.
7. **Install S01 probe only into new test project.** In the Script editor add `OutboundGuard` and `S01Probe` script files using the two `.js` sources (the editor uses `.gs` names). Enable manifest visibility and copy `appsscript.json`. There is no deployment endpoint or trigger to install. Have the company test connection owner review/authorise the manifest's mail-send, Calendar and Script scopes; Calendar scope is broad, so the executing account must have no production calendars. Do not run as a developer's personal account. These are test-only privileges; never deploy this harness to PROD.
8. **Configure capture.** In that project's Script Properties, set `S01_CONFIG` to a protected copy of `environment.example.json`, with `environment: TEST`, actual matching `scriptProjectId`, verified test resource IDs, exact allowedRecipients/allowedCalendarIds, `mode: CAPTURE`, `probeEnabled: false`. Set `S01_REQUEST` to one request shown below. Execute `runS01Probe` manually. Approved capture returns CAPTURED, never receipt. An invalid/missing/mixed config must be corrected before proceeding.
9. **Execute refusal checks.** In the verified isolated project set `mode: PROBE`, `probeEnabled: true`. For G01-01 use EMAIL with `to: ["outside@example.invalid"]`, not on the allowlist; expect S01_REFUSED before MailApp is called. For G01-02 use CALENDAR with that same unapproved address and the verified test calendarId; expect refusal and no event/invite. Capture request/config version, execution error/log and mailbox/calendar observations. Repeat with a mixed approved/unapproved list and with unapproved cc/bcc as supplemental checks. Never use an actual customer as the negative-test recipient.
10. **Execute delivery checks.** Replace `to` with one approved real test inbox. Run EMAIL once (G01-03), then CALENDAR once (G01-04) with the verified secondary test calendar. Email request is `{"kind":"EMAIL","to":["approved@example.invalid"],"cc":[],"bcc":[]}`; Calendar is `{"kind":"CALENDAR","to":["approved@example.invalid"],"calendarId":"synthetic-test-calendar"}`. These examples must have approved real test values substituted privately for delivery. Messages contain only a synthetic label; Calendar probe is a 15-minute event 24 hours after execution, not a business scheduling rule. Record probe label, execution result, event ID where returned and independent recipient receipt screenshot/time. SUBMITTED is not PASS: mailbox receipt and invitation details must be confirmed. Inspect for duplicate/uncertain sends before retry; no automatic retries. Return config to CAPTURE/disabled after testing.
11. **Prove production denial (G01-05).** Ben supplies the production data folder link privately. In a clean separate browser profile signed in solely as the restricted test user, open it directly. Capture denial plus the test identity/time. Also inspect direct source/file permissions and AppSheet sources to exclude production exposure through the service account. Do not enumerate/read customer records. If access unexpectedly succeeds, record FAIL and report it to Ben; do not alter production ACLs in S01. Resolve isolation through new test identities/resources or an explicitly authorised follow-up.
12. **Prove company access (G01-06/07).** Ben signs in with his own company account, opens the source repository and the actual project/integration inventory, and confirms company recovery/control survives developer departure. Capture both accesses separately, not a developer's screenshot of sharing settings alone. Keep account details in restricted evidence.
13. **Close the gate.** Fill actual results/tester/time/release/config/evidence in the register; log observed failures with severity, owner and retest. Finish environment, ownership, entitlement, export and fixture documentation evidence. All G01 rows and S01 setup conditions must pass before the developer records permission to progress. Under RA01, Ben's go-live decision is required for each R?-S20 after that release's acceptance and migration/training; all modules need not finish before R1. Do not begin S02 automatically.

Code guards enforce exact destinations and target IDs, not cloud ACLs, mailbox forwarding or truth of an administrator's allowlist. Those require the independent setup/identity evidence above. Script editors can change code/properties; they are trusted privileged users, not ordinary app users. No claim of cloud isolation is made from local tests.

Technical references checked for this harness: [Calendar createEvent options and ownership methods](https://developers.google.com/apps-script/reference/calendar/calendar), [MailApp sendEmail](https://developers.google.com/apps-script/reference/mail/mail-app), and [AppSheet security filters](https://support.google.com/appsheet/answer/10104488). Provider documentation supports the API design, not the actual company's plan entitlement or successful execution.
