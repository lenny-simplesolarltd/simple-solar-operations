/* Generated by npm run build:installer. DEV installer mobile workflow (FN-06 R3). No external calls. */
var IW_HEADERS={"Jobs":["id","job_id","customer_id","display_name","sold_submission_id","booking_submission_id","sold_at","salesperson_id","lead_source","quote_reference","presale_file_id","finance_route","contract_status","contract_id","contract_signed_at","contract_evidence_id","original_net_pence","original_vat_pence","original_gross_pence","approved_change_pence","current_contract_gross_pence","valuation_basis","sold_booking_match_status","customer_details_verified_at","customer_details_verified_by","deposit_bank_confirmed_at","deposit_bank_confirmed_by","deposit_bank_reference","roof_required","electrical_required","scaffold_required","workflow_stage","booking_approved_at","booking_approved_by","operational_complete_at","operational_complete_by","customer_happy_at","customer_happy_by","handover_status","financial_status","cancellation_at","cancellation_by","cancellation_reason","archived_at","next_action_at","account_policy_version","pilot_job","release_scope","created_at","created_by","updated_at","updated_by","version","source_system","source_record_id","commit_id"],"Customers":["id","first_name","last_name","address_line1","address_line2","town","postcode","email","phone","alternate_contact","contact_notes","created_at","created_by","updated_at","updated_by","version","source_system","source_record_id","commit_id"],"WorkPackages":["id","job_id","trade","required","planned_start","planned_end","actual_start","actual_end","status","need_by_date","completion_outcome","installer_confirmation_at","installer_confirmation_by","commissioning_required","sequence","revision","parent_package_id","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Allocations":["id","work_package_id","person_id","role","start_at","end_at","active","replaced_allocation_id","cancellation_reason","calendar_link_id","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"People":["id","email","display_name","role","company_id","active","calendar_id","notification_email","capacity_per_day","available_from","available_to","backup_person_id","created_at","created_by","updated_at","updated_by","version","source_system","source_record_id","commit_id"],"PersonRoles":["id","person_id","role","active","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Tasks":["id","job_id","template_code","instance_key","group","title","owner_id","backup_id","related_entity_type","related_entity_id","due_at","original_due_at","priority","status","blocking_reason","next_followup_at","completed_at","completed_by","completion_note","evidence_id","revision_required","created_rule_version","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"TaskTemplates":["id","template_code","title","group","default_owner_role","trigger_event","due_rule","evidence_required","active","template_version","created_at","created_by","updated_at","updated_by","version","commit_id"],"Issues":["id","job_id","work_package_id","type","category","description","raised_at","raised_by","responsible_person_id","responsible_company_id","office_owner_id","severity","status","due_at","next_followup_at","blocks_completion","blocks_strip","estimated_value_pence","approved_value_pence","approval_status","approved_at","approved_by","resolution","resolved_at","closed_at","closed_by","customer_resolution_confirmed","linked_return_package_id","evidence_folder_id","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"IssueEvents":["id","issue_id","event_type","actor","timestamp","note","previous_status","new_status","evidence_id","created_at","commit_id"],"Evidence":["id","job_id","submission_id","issue_id","category","drive_file_id","filename","mime_type","upload_status","captured_at","captured_by","received_at","customer_shareable","version","checksum","created_at","commit_id"],"CommissioningSubmissions":["id","job_id","work_package_id","allocation_id","installer_id","template_version","status","submitted_at","reviewed_at","reviewed_by","review_notes","supersedes_submission_id","created_at","created_by","updated_at","updated_by","version","commit_id"],"CommissioningAnswers":["id","submission_id","question_key","value_text","value_number","value_date","value_boolean","not_applicable_reason","created_at","commit_id"],"Settings":["id","key","typed_value","scope","version","effective_from","changed_by","reason","created_at","commit_id"],"Holidays":["id","local_date","description","office_closed","created_at","created_by","commit_id"],"CommitJournal":["id","commit_id","state","command_id","entity_type","entity_id","expected_version","changes_json","prepared_at","committed_at","created_at"],"AuditEvents":["id","entity_type","entity_id","action","before_json","after_json","initiating_actor","executing_service","timestamp","correlation_id","reason","commit_id","created_at"],"ReleaseModes":["id","function_id","function_name","mode","mode_record_basis","authorised_job_scope","target_release","planned_target_mode","current_system","fallback","external_ids_protected_reference","activation_time","approved_version","ben_approval_reference","scope_boundary_notes","created_at","created_by","updated_at","updated_by","version","commit_id"]};
/* Installer mobile workflow — assigned work, start/progress, completion outcome, evidence, remedials/problems/variations,
 * commissioning draft/submit/resubmit. Authority: 01 §4 WorkPackages (status Unscheduled/Scheduled/InProgress/ReportedComplete/
 * ConfirmedComplete/ReturnRequired/Cancelled, actual_start/actual_end, completion_outcome), Submissions (Draft/Submitted/UnderReview/
 * Returned/Accepted, supersedes_submission_id), Evidence; 01 ("installers see assigned work only", "required trades must be
 * individually reported and then confirmed", "exclude financial and private issue records from installer access"); 04 S12
 * ("allow installer progress, variation, problem and return reports before the final form is submitted", "ReturnRequired → remedial
 * plus Tanya's customer date-booking task; link a return package to the original work and retain the original installer's
 * responsibility", "duplicate sync", "cross-job file denial", "immutable versions"); AGENT_RUNBOOK §E (no commissioning
 * certification requirements invented — templates/questions stay NOT_CONFIGURED until approved).
 * FN-06 Installer app/forms access (R3). Office confirmation (ConfirmedComplete) and INS01/INS02 stay with S10. Return packages keep
 * the original trade (Roof/Electrical) with parent_package_id — no new trade strings. No external calls. */

var IW_DEV_SHEET_ID = '1z7PNZtDdC4Z5eLbmTuQdqp0QpJSmuEvx3QvN3VyNTsc';
var IW_SERVICE = 'InstallerWorkflow';
var IW_OUTCOMES = ['Complete', 'ReturnRequired'];
var IW_PROBLEM_CATEGORIES = ['Access', 'Damage', 'Technical', 'Safety', 'MaterialsShort', 'Other'];
var IW_EVIDENCE_CATEGORIES = ['Progress', 'Completion', 'Commissioning', 'Problem', 'Variation', 'Return'];
var IW_TEMPLATES = {
  BKG02: { title: 'Book dates and allocations', group: 'Booking', role: 'Office' },
  REM01: { title: 'Arrange remedial return visit', group: 'Aftercare', role: 'Office' },
  ISS01: { title: 'Review variation', group: 'Aftercare', role: 'VariationApprover' },
  ISS02: { title: 'Review installer problem report', group: 'Aftercare', role: 'Office' }
};

/* --- utilities --- */

function _iwText(x) { return typeof x === 'string' && x.trim().length > 0; }
function _iwTrue(v) { return v === true || v === 'TRUE' || v === 'true' || v === 1; }
function _iwRefuse(code) { var e = new Error(code); e.code = code; throw e; }
function _iwDate(value) {
  if (value === null || value === undefined || value === '') return null;
  if (Object.prototype.toString.call(value) === '[object Date]') { if (isNaN(value.getTime())) _iwRefuse('IW_DATE_INVALID'); if (typeof Utilities !== 'undefined' && Utilities.formatDate) return Utilities.formatDate(value, 'Europe/London', 'yyyy-MM-dd'); return new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/London', year: 'numeric', month: '2-digit', day: '2-digit' }).format(value); }
  var m = /^(\d{4})-(\d{2})-(\d{2})/.exec(String(value).trim()); if (!m) _iwRefuse('IW_DATE_INVALID');
  var iso = m[1] + '-' + m[2] + '-' + m[3], d = new Date(iso + 'T12:00:00Z'); if (isNaN(d.getTime()) || d.toISOString().slice(0, 10) !== iso) _iwRefuse('IW_DATE_INVALID'); return iso;
}
function _iwNow(input) { if (input && input.at) { var d = new Date(input.at); if (isNaN(d.getTime())) _iwRefuse('IW_DATE_INVALID'); return d.toISOString(); } return new Date().toISOString(); }
function _iwLocalToday(iso) { return new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/London', year: 'numeric', month: '2-digit', day: '2-digit' }).format(new Date(iso)); }
function _iwAddDays(iso, n) { var d = new Date(iso + 'T12:00:00Z'); d.setUTCDate(d.getUTCDate() + n); return d.toISOString().slice(0, 10); }
function _iwSetting(store, key, fallback) { var rows = store.list('Settings').filter(function (s) { return s.key === key; }); if (!rows.length) return fallback; rows.sort(function (a, b) { return Number(b.version || 0) - Number(a.version || 0); }); try { return JSON.parse(rows[0].typed_value); } catch (e) { return fallback; } }
function _iwIsStaffed(store, iso) { var wd = _iwSetting(store, 'office.staffed_weekdays', [1, 2, 3, 4, 5]), closed = store.list('Holidays').filter(function (h) { return _iwTrue(h.office_closed); }).map(function (h) { try { return _iwDate(h.local_date); } catch (e) { return null; } }); return wd.indexOf(new Date(iso + 'T12:00:00Z').getUTCDay()) !== -1 && closed.indexOf(iso) === -1; }
function _iwNextStaffed(store, iso) { var d = _iwAddDays(iso, 1), n = 0; while (!_iwIsStaffed(store, d) && n < 60) { d = _iwAddDays(d, 1); n++; } return d; }
function _iwSameOrNextStaffed(store, iso) { return _iwIsStaffed(store, iso) ? iso : _iwNextStaffed(store, iso); }
function _iwLondon(iso, hhmm) { var parts = hhmm.split(':'), guess = new Date(iso + 'T' + hhmm + ':00Z'), map = {}; new Intl.DateTimeFormat('en-GB', { timeZone: 'Europe/London', hourCycle: 'h23', hour: '2-digit', minute: '2-digit' }).formatToParts(guess).forEach(function (p) { map[p.type] = p.value; }); var off = (Number(map.hour === '24' ? 0 : map.hour) * 60 + Number(map.minute)) - (Number(parts[0]) * 60 + Number(parts[1])); return new Date(guess.getTime() - off * 60000).toISOString(); }
function _iwHash(text) { var h = 0; for (var i = 0; i < text.length; i++) { h = ((h << 5) - h + text.charCodeAt(i)) | 0; } return (h >>> 0).toString(16).padStart(8, '0'); }

/* --- guards --- */

function _iwGuardStore(store) { if (!store || !store.getSheetId || store.getSheetId() !== IW_DEV_SHEET_ID || !store.getEnvironment || store.getEnvironment() !== 'DEV') _iwRefuse('IW_REFUSED: exact DEV sheet/environment required'); }
function _iwRequirePilot(store) { var rows = store.list('ReleaseModes').filter(function (r) { return r.function_id === 'FN-06'; }); if (rows.length !== 1 || rows[0].target_release !== 'R3') _iwRefuse('IW_REFUSED: FN-06 ReleaseMode invalid'); if (rows[0].mode !== 'Automated' || rows[0].authorised_job_scope !== 'Pilot') _iwRefuse('IW_REFUSED: FN-06 must be Automated/Pilot/R3 (got ' + rows[0].mode + '/' + rows[0].authorised_job_scope + ')'); }
function _iwPerson(store, id) { var p = store.get('People', id); if (!p || !_iwTrue(p.active)) _iwRefuse('IW_REFUSED: actor must be an active person id'); return p; }
function _iwRoles(store, p) { return [p.role].concat(store.list('PersonRoles').filter(function (r) { return r.person_id === p.id && _iwTrue(r.active); }).map(function (r) { return r.role; })); }
function _iwIsOffice(store, p) { return _iwRoles(store, p).some(function (r) { return ['Admin', 'Manager', 'Office'].indexOf(r) !== -1; }); }
function _iwPackage(store, wpId) { var wp = store.get('WorkPackages', wpId); if (!wp) _iwRefuse('IW_REVIEW: work package not found'); var job = store.get('Jobs', wp.job_id); if (!job) _iwRefuse('IW_REVIEW: job not found'); if (!_iwTrue(job.pilot_job)) _iwRefuse('IW_REFUSED: pilot job required'); if (job.cancellation_at || ['CancellationInProgress', 'Cancelled'].indexOf(job.workflow_stage) !== -1) _iwRefuse('S15_REVIEW: normal work suppressed'); return { wp: wp, job: job }; }
function _iwAllocationsFor(store, wpId) { return store.list('Allocations').filter(function (a) { return a.work_package_id === wpId && _iwTrue(a.active); }); }
/* Installers may act only on packages they are allocated to ("assigned work only"); office roles may act with a reason. */
function _iwAssertAccess(store, actor, wp, input) {
  var p = _iwPerson(store, actor);
  var allocated = _iwAllocationsFor(store, wp.id).some(function (a) { return a.person_id === p.id; });
  if (allocated) return { person: p, via: 'allocation' };
  if (_iwIsOffice(store, p)) { if (!_iwText(input && input.reason)) _iwRefuse('IW_REVIEW: office actor must give a reason when acting on an installer package'); return { person: p, via: 'office' }; }
  _iwRefuse('IW_REFUSED: actor is not allocated to this work package');
}
function _iwCommandStart(store, input, entityType, entityId, changes) {
  if (!_iwText(input.command_id) || !_iwText(input.actor)) _iwRefuse('IW_REVIEW: command_id and actor required');
  var id = 'CJ-IW-' + input.command_id, encoded = JSON.stringify(changes), existing = store.get('CommitJournal', id);
  if (existing) { if (existing.command_id !== input.command_id || existing.entity_type !== entityType || existing.entity_id !== entityId || existing.changes_json !== encoded) _iwRefuse('IW_REVIEW: conflicting command identity'); if (existing.state !== 'Committed') _iwRefuse('IW_RECOVERY_REQUIRED: incomplete command ' + input.command_id); return { replay: true }; }
  var now = _iwNow(input);
  store.insert('CommitJournal', { id: id, commit_id: 'IW-' + input.command_id, state: 'Prepared', command_id: input.command_id, entity_type: entityType, entity_id: entityId, expected_version: input.expected_version === undefined ? null : input.expected_version, changes_json: encoded, prepared_at: now, committed_at: null, created_at: now });
  return { replay: false };
}
function _iwCommit(store, input, now) { store.update('CommitJournal', 'CJ-IW-' + input.command_id, { state: 'Committed', committed_at: now }); }
function _iwAudit(store, type, id, action, before, after, input, now) { store.insert('AuditEvents', { id: 'AUD-IW-' + input.command_id + '-' + type + '-' + id + '-' + action, entity_type: type, entity_id: id, action: action, before_json: before ? JSON.stringify(before) : null, after_json: after ? JSON.stringify(after) : null, initiating_actor: input.actor, executing_service: IW_SERVICE, timestamp: now, correlation_id: input.command_id, reason: input.reason || input.note || null, commit_id: 'IW-' + input.command_id, created_at: now }); }
function _iwExpect(row, input) { if (input.expected_version === undefined || input.expected_version === null) _iwRefuse('IW_REVIEW: expected_version required'); if (Number(row.version) !== Number(input.expected_version)) _iwRefuse('IW_STALE: version'); }
function _iwPatch(store, table, row, patch, input, now) { var p = {}; for (var k in patch) if (patch.hasOwnProperty(k)) p[k] = patch[k]; p.updated_at = now; p.updated_by = input.actor; p.version = Number(row.version || 0) + 1; store.update(table, row.id, p); return store.get(table, row.id); }
function _iwOwnerForRole(store, role) {
  var people = store.list('People').filter(function (p) { return _iwTrue(p.active); });
  var byRole = people.filter(function (p) { return _iwRoles(store, p).indexOf(role) !== -1; });
  if (role === 'Office') { var tanya = byRole.filter(function (p) { return String(p.display_name || '').toLowerCase().indexOf('tanya') !== -1; })[0]; if (tanya) return tanya; }
  if (byRole.length) return byRole[0];
  if (role !== 'Office') return _iwOwnerForRole(store, 'Office');
  _iwRefuse('IW_CONFIG: active Office owner required');
}
function _iwTask(store, code, jobId, related, instanceKey, dueAt, input, now, suffix) {
  var existing = store.list('Tasks').filter(function (t) { return t.instance_key === instanceKey && ['Complete', 'Cancelled', 'NotRequired'].indexOf(t.status) === -1; });
  if (existing.length) return { created: false, task_id: existing[0].id, code: code };
  var tpl = store.list('TaskTemplates').filter(function (t) { return t.template_code === code && _iwTrue(t.active); })[0], def = IW_TEMPLATES[code], owner = _iwOwnerForRole(store, (tpl && tpl.default_owner_role) || def.role);
  var task = { id: 'TASK-IW-' + instanceKey, job_id: jobId, template_code: code, instance_key: instanceKey, group: (tpl && tpl.group) || def.group, title: ((tpl && tpl.title) || def.title) + (suffix ? ' — ' + suffix : ''), owner_id: owner.id, backup_id: owner.backup_person_id || null, related_entity_type: related.type, related_entity_id: related.id, due_at: dueAt, original_due_at: dueAt, priority: 1, status: 'Open', blocking_reason: null, next_followup_at: null, completed_at: null, completed_by: null, completion_note: null, evidence_id: null, revision_required: false, created_rule_version: 'IW-1.0', created_at: now, created_by: input.actor, updated_at: now, updated_by: input.actor, version: 1, source_system: IW_SERVICE, commit_id: 'IW-' + input.command_id };
  store.insert('Tasks', task);
  return { created: true, task_id: task.id, code: code, owner_id: owner.id };
}
function _iwIssue(store, input, job, wp, type, category, description, owner, extra, now) {
  var id = 'ISS-IW-' + input.command_id;
  if (store.get('Issues', id)) return { created: false, issue: store.get('Issues', id) };
  var issue = Object.assign({ id: id, job_id: job.id, work_package_id: wp ? wp.id : null, type: type, category: category, description: description, raised_at: now, raised_by: input.actor, responsible_person_id: null, responsible_company_id: null, office_owner_id: owner.id, severity: 'Normal', status: 'Open', due_at: _iwLondon(_iwNextStaffed(store, _iwLocalToday(now)), '09:00'), next_followup_at: null, blocks_completion: true, blocks_strip: false, estimated_value_pence: null, approved_value_pence: null, approval_status: type === 'Variation' ? 'Pending' : 'NotRequired', approved_at: null, approved_by: null, resolution: null, resolved_at: null, closed_at: null, closed_by: null, customer_resolution_confirmed: null, linked_return_package_id: null, evidence_folder_id: null, created_at: now, created_by: input.actor, updated_at: now, updated_by: input.actor, version: 1, source_system: IW_SERVICE, commit_id: 'IW-' + input.command_id }, extra || {});
  store.insert('Issues', issue);
  store.insert('IssueEvents', { id: 'IE-' + id + '-OPEN', issue_id: id, event_type: 'Opened', actor: input.actor, timestamp: now, note: description, previous_status: null, new_status: 'Open', evidence_id: null, created_at: now, commit_id: 'IW-' + input.command_id });
  return { created: true, issue: issue };
}
function _iwEvidenceRows(store, input, job, wp, items, category, submissionId, issueId, now) {
  var out = [];
  (items || []).forEach(function (it) {
    if (!it || !_iwText(it.drive_file_id)) _iwRefuse('IW_REVIEW: evidence drive_file_id required');
    var cat = it.category || category; if (IW_EVIDENCE_CATEGORIES.indexOf(cat) === -1) _iwRefuse('IW_REVIEW: evidence category must be one of ' + IW_EVIDENCE_CATEGORIES.join('/'));
    var id = 'EV-IW-' + wp.id + '-' + _iwHash(it.drive_file_id.trim());
    var existing = store.get('Evidence', id);
    if (existing) { if (existing.job_id !== job.id) _iwRefuse('IW_REFUSED: evidence file already linked to another job'); out.push({ evidence_id: id, created: false }); return; }
    store.insert('Evidence', { id: id, job_id: job.id, submission_id: submissionId || null, issue_id: issueId || null, category: cat, drive_file_id: it.drive_file_id.trim(), filename: _iwText(it.filename) ? it.filename.trim() : (cat.toLowerCase() + '-' + wp.id), mime_type: it.mime_type || null, upload_status: 'Uploaded', captured_at: it.captured_at || now, captured_by: input.actor, received_at: now, customer_shareable: it.customer_shareable === true, version: 1, checksum: it.checksum || null, created_at: now, commit_id: 'IW-' + input.command_id });
    out.push({ evidence_id: id, created: true });
  });
  return out;
}

/* --- 1. MY WORK (installer-safe read: no financial or private issue data) --- */

function _iwMyWork(store, personId, input) {
  _iwGuardStore(store);
  input = input || {};
  var p = _iwPerson(store, personId), from = _iwDate(input.from) || null, to = _iwDate(input.to) || null;
  var allocations = store.list('Allocations').filter(function (a) { return a.person_id === p.id && _iwTrue(a.active); });
  var items = [];
  allocations.forEach(function (a) {
    var wp = store.get('WorkPackages', a.work_package_id); if (!wp || wp.status === 'Cancelled') return;
    var start = wp.planned_start ? _iwDate(wp.planned_start) : null, end = wp.planned_end ? _iwDate(wp.planned_end) : null;
    if (from && end && end < from) return; if (to && start && start > to) return;
    var job = store.get('Jobs', wp.job_id) || {}; if (job.cancellation_at) return;
    var customer = job.customer_id ? store.get('Customers', job.customer_id) : null;
    var sub = store.list('CommissioningSubmissions').filter(function (s) { return s.work_package_id === wp.id; }).sort(function (x, y) { return String(y.created_at).localeCompare(String(x.created_at)); })[0] || null;
    var issues = store.list('Issues').filter(function (i) { return i.work_package_id === wp.id && ['Resolved', 'Closed'].indexOf(i.status) === -1; });
    items.push({ allocation_id: a.id, role: a.role, work_package_id: wp.id, trade: wp.trade, status: wp.status, planned_start: start, planned_end: end, actual_start: wp.actual_start ? _iwDate(wp.actual_start) : null, actual_end: wp.actual_end ? _iwDate(wp.actual_end) : null, completion_outcome: wp.completion_outcome || null, commissioning_required: _iwTrue(wp.commissioning_required), expected_version: wp.version, parent_package_id: wp.parent_package_id || null,
      job: { id: job.id, display_name: job.display_name, job_reference: job.job_id, workflow_stage: job.workflow_stage }, site: customer ? { address_line1: customer.address_line1, town: customer.town, postcode: customer.postcode, phone: customer.phone || null } : null,
      commissioning: sub ? { submission_id: sub.id, status: sub.status, submitted_at: sub.submitted_at || null, review_notes: sub.status === 'Returned' ? sub.review_notes : null } : null,
      evidence_count: store.list('Evidence').filter(function (e) { return e.job_id === job.id && String(e.id).indexOf('EV-IW-' + wp.id + '-') === 0; }).length,
      open_issues: issues.map(function (i) { return { id: i.id, type: i.type, category: i.category, status: i.status, raised_by_me: i.raised_by === p.id }; }),
      my_tasks: store.list('Tasks').filter(function (t) { return t.owner_id === p.id && t.related_entity_id === wp.id && ['Complete', 'Cancelled', 'NotRequired'].indexOf(t.status) === -1; }).map(function (t) { return { id: t.id, template_code: t.template_code, title: t.title, due_at: t.due_at }; }) });
  });
  items.sort(function (a, b) { return String(a.planned_start || '9999').localeCompare(String(b.planned_start || '9999')); });
  return { person_id: p.id, display_name: p.display_name, count: items.length, items: items, excluded: ['finance', 'invoices', 'private issue notes'] };
}

/* --- 2. START / PROGRESS --- */

function _iwStart(store, input) {
  _iwGuardStore(store); _iwRequirePilot(store);
  var ctx = _iwPackage(store, input.work_package_id), wp = ctx.wp; _iwAssertAccess(store, input.actor, wp, input);
  var cmd = _iwCommandStart(store, input, 'WorkPackages', wp.id, { action: 'Start' }); if (cmd.replay) return { replay: true, status: wp.status, work_package: wp };
  if (['Scheduled', 'InProgress', 'ReturnRequired'].indexOf(wp.status) === -1) _iwRefuse('IW_REVIEW: cannot start from ' + wp.status);
  _iwExpect(wp, input);
  var now = _iwNow(input), today = _iwLocalToday(now);
  var after = _iwPatch(store, 'WorkPackages', wp, { status: 'InProgress', actual_start: wp.actual_start || today }, input, now);
  _iwAudit(store, 'WorkPackages', wp.id, 'InstallerStart', wp, after, input, now); _iwCommit(store, input, now);
  return { replay: false, status: after.status, work_package: after, external_calls: 0 };
}
function _iwProgress(store, input) {
  _iwGuardStore(store); _iwRequirePilot(store);
  var ctx = _iwPackage(store, input.work_package_id), wp = ctx.wp; _iwAssertAccess(store, input.actor, wp, input);
  if (!_iwText(input.note) && !(Array.isArray(input.evidence) && input.evidence.length)) _iwRefuse('IW_REVIEW: note or evidence required');
  var cmd = _iwCommandStart(store, input, 'WorkPackages', wp.id, { action: 'Progress', note: input.note || null, files: (input.evidence || []).map(function (e) { return e && e.drive_file_id; }) }); if (cmd.replay) return { replay: true };
  if (['InProgress', 'Scheduled', 'ReturnRequired'].indexOf(wp.status) === -1) _iwRefuse('IW_REVIEW: progress can only be reported on open work (status ' + wp.status + ')');
  var now = _iwNow(input), ev = _iwEvidenceRows(store, input, ctx.job, wp, input.evidence, 'Progress', null, null, now);
  _iwAudit(store, 'WorkPackages', wp.id, 'InstallerProgress', null, { note: input.note || null, percent: input.percent === undefined ? null : Number(input.percent), evidence: ev.map(function (e) { return e.evidence_id; }) }, input, now); _iwCommit(store, input, now);
  return { replay: false, evidence: ev, external_calls: 0 };
}

/* --- 3. REPORT COMPLETION OUTCOME (Complete → ReportedComplete; ReturnRequired → remedial + return package) --- */

function _iwReportCompletion(store, input) {
  _iwGuardStore(store); _iwRequirePilot(store);
  var ctx = _iwPackage(store, input.work_package_id), wp = ctx.wp, job = ctx.job, access = _iwAssertAccess(store, input.actor, wp, input);
  if (IW_OUTCOMES.indexOf(input.outcome) === -1) _iwRefuse('IW_REVIEW: outcome must be Complete or ReturnRequired');
  var actualEnd = _iwDate(input.actual_end); if (!actualEnd) _iwRefuse('IW_REVIEW: actual_end required');
  if (input.outcome === 'ReturnRequired' && !_iwText(input.return_reason)) _iwRefuse('IW_REVIEW: return_reason required');
  var cmd = _iwCommandStart(store, input, 'WorkPackages', wp.id, { action: 'ReportCompletion', outcome: input.outcome, actual_end: actualEnd, return_reason: input.return_reason || null }); if (cmd.replay) return { replay: true, status: store.get('WorkPackages', wp.id).status };
  if (['InProgress', 'Scheduled', 'ReturnRequired'].indexOf(wp.status) === -1) _iwRefuse('IW_REVIEW: cannot report completion from ' + wp.status);
  _iwExpect(wp, input);
  var now = _iwNow(input), today = _iwLocalToday(now); if (actualEnd > today) _iwRefuse('IW_REVIEW: actual_end cannot be in the future');
  var installer = access.via === 'allocation' ? access.person : (store.get('People', (_iwAllocationsFor(store, wp.id).filter(function (a) { return a.role === 'Lead'; })[0] || _iwAllocationsFor(store, wp.id)[0] || {}).person_id) || access.person);
  var ev = _iwEvidenceRows(store, input, job, wp, input.evidence, input.outcome === 'Complete' ? 'Completion' : 'Return', null, null, now);
  var result = { replay: false, outcome: input.outcome, evidence: ev, external_calls: 0 };
  if (input.outcome === 'Complete') {
    var after = _iwPatch(store, 'WorkPackages', wp, { status: 'ReportedComplete', completion_outcome: 'Complete', actual_start: wp.actual_start || actualEnd, actual_end: actualEnd }, input, now);
    var submission = null;
    if (_iwTrue(wp.commissioning_required)) {
      var subId = 'CS-' + wp.id; submission = store.get('CommissioningSubmissions', subId);
      if (!submission) { submission = { id: subId, job_id: job.id, work_package_id: wp.id, allocation_id: (_iwAllocationsFor(store, wp.id).filter(function (a) { return a.person_id === installer.id; })[0] || {}).id || null, installer_id: installer.id, template_version: input.template_version || 'NOT_CONFIGURED', status: 'Draft', submitted_at: null, reviewed_at: null, reviewed_by: null, review_notes: null, supersedes_submission_id: null, created_at: now, created_by: input.actor, updated_at: now, updated_by: input.actor, version: 1, commit_id: 'IW-' + input.command_id }; store.insert('CommissioningSubmissions', submission); }
    }
    _iwAudit(store, 'WorkPackages', wp.id, 'InstallerReportedComplete', wp, after, input, now);
    result.status = after.status; result.work_package = after; result.commissioning_submission = submission ? { id: submission.id, status: submission.status } : null;
    result.next = 'Office confirmation call (INS01) is scheduled by S10 from the reported completion; ' + (submission ? 'commissioning form ' + submission.status + ' — submit within two working days' : 'no commissioning required');
  } else {
    var after2 = _iwPatch(store, 'WorkPackages', wp, { status: 'ReturnRequired', completion_outcome: 'ReturnRequired', actual_start: wp.actual_start || actualEnd, actual_end: actualEnd }, input, now);
    var owner = _iwOwnerForRole(store, 'Office');
    var returnId = wp.id + '-RET' + (store.list('WorkPackages').filter(function (w) { return w.parent_package_id === wp.id; }).length + 1);
    var maxSeq = store.list('WorkPackages').filter(function (w) { return w.job_id === job.id; }).reduce(function (m, w) { return Math.max(m, Number(w.sequence || 0)); }, 0);
    var ret = { id: returnId, job_id: job.id, trade: wp.trade, required: true, planned_start: null, planned_end: null, actual_start: null, actual_end: null, status: 'Unscheduled', need_by_date: null, completion_outcome: null, installer_confirmation_at: null, installer_confirmation_by: null, commissioning_required: _iwTrue(wp.commissioning_required), sequence: maxSeq + 1, revision: 1, parent_package_id: wp.id, created_at: now, created_by: input.actor, updated_at: now, updated_by: input.actor, version: 1, source_system: IW_SERVICE, commit_id: 'IW-' + input.command_id };
    store.insert('WorkPackages', ret);
    /* Original installer keeps responsibility: dateless allocation on the return package (planner assigns dates via S11). */
    var allocId = 'ALLOC-' + returnId; store.insert('Allocations', { id: allocId, work_package_id: returnId, person_id: installer.id, role: 'Lead', start_at: null, end_at: null, active: true, replaced_allocation_id: null, cancellation_reason: null, calendar_link_id: null, created_at: now, created_by: input.actor, updated_at: now, updated_by: input.actor, version: 1, source_system: IW_SERVICE, commit_id: 'IW-' + input.command_id });
    var issue = _iwIssue(store, input, job, wp, 'Remedial', 'ReturnRequired', input.return_reason.trim(), owner, { responsible_person_id: installer.id, linked_return_package_id: returnId, blocks_completion: true }, now);
    ev.forEach(function (e) { if (e.created) store.update('Evidence', e.evidence_id, { issue_id: issue.issue.id }); });
    var remedial = _iwTask(store, 'REM01', job.id, { type: 'Issues', id: issue.issue.id }, 'REM01-' + issue.issue.id, issue.issue.due_at, input, now, wp.trade + ' return visit');
    var booking = _iwTask(store, 'BKG02', job.id, { type: 'WorkPackages', id: returnId }, 'BKG02-' + returnId, _iwLondon(_iwNextStaffed(store, today), '09:00'), input, now, 'customer date for return visit ' + returnId);
    _iwAudit(store, 'WorkPackages', wp.id, 'InstallerReturnRequired', wp, after2, input, now);
    result.status = after2.status; result.work_package = after2; result.return_package = ret; result.return_allocation_id = allocId; result.issue = issue.issue; result.tasks = [remedial, booking];
    result.next = 'Remedial raised; Tanya books the return visit date (BKG02); original installer remains responsible; INS01 call scheduled by S10.';
  }
  _iwCommit(store, input, now);
  return result;
}

/* --- 4. PROBLEM / VARIATION REPORTS (before the final form) --- */

function _iwReportProblem(store, input) {
  _iwGuardStore(store); _iwRequirePilot(store);
  var ctx = _iwPackage(store, input.work_package_id), wp = ctx.wp, job = ctx.job; _iwAssertAccess(store, input.actor, wp, input);
  if (IW_PROBLEM_CATEGORIES.indexOf(input.category) === -1) _iwRefuse('IW_REVIEW: category must be one of ' + IW_PROBLEM_CATEGORIES.join('/'));
  if (!_iwText(input.description)) _iwRefuse('IW_REVIEW: description required');
  var cmd = _iwCommandStart(store, input, 'WorkPackages', wp.id, { action: 'Problem', category: input.category, description: input.description }); if (cmd.replay) return { replay: true, issue_id: 'ISS-IW-' + input.command_id };
  var now = _iwNow(input), owner = _iwOwnerForRole(store, 'Office');
  var blocks = input.blocks_completion === undefined ? (input.category === 'Safety' || input.category === 'Technical') : input.blocks_completion === true;
  var issue = _iwIssue(store, input, job, wp, 'Remedial', input.category, input.description.trim(), owner, { blocks_completion: blocks, severity: input.category === 'Safety' ? 'High' : 'Normal' }, now);
  var ev = _iwEvidenceRows(store, input, job, wp, input.evidence, 'Problem', null, issue.issue.id, now);
  var task = _iwTask(store, 'ISS02', job.id, { type: 'Issues', id: issue.issue.id }, 'ISS02-' + issue.issue.id, issue.issue.due_at, input, now, input.category + ' on ' + wp.trade);
  _iwCommit(store, input, now);
  return { replay: false, issue: issue.issue, blocks_completion: blocks, evidence: ev, task: task, external_calls: 0 };
}
function _iwReportVariation(store, input) {
  _iwGuardStore(store); _iwRequirePilot(store);
  var ctx = _iwPackage(store, input.work_package_id), wp = ctx.wp, job = ctx.job; _iwAssertAccess(store, input.actor, wp, input);
  if (!_iwText(input.description)) _iwRefuse('IW_REVIEW: description required');
  var cmd = _iwCommandStart(store, input, 'WorkPackages', wp.id, { action: 'Variation', description: input.description, estimated_value_pence: input.estimated_value_pence === undefined ? null : Number(input.estimated_value_pence) }); if (cmd.replay) return { replay: true, issue_id: 'ISS-IW-' + input.command_id };
  var now = _iwNow(input), owner = _iwOwnerForRole(store, 'VariationApprover');
  var est = input.estimated_value_pence === undefined || input.estimated_value_pence === null ? null : Number(input.estimated_value_pence); if (est !== null && (!Number.isInteger(est) || est < 0)) _iwRefuse('IW_REVIEW: estimated_value_pence must be a non-negative integer');
  var issue = _iwIssue(store, input, job, wp, 'Variation', 'Variation', input.description.trim(), owner, { estimated_value_pence: est, blocks_completion: false, approval_status: 'Pending', due_at: _iwLondon(_iwSameOrNextStaffed(store, _iwLocalToday(now)), '17:00') }, now);
  var ev = _iwEvidenceRows(store, input, job, wp, input.evidence, 'Variation', null, issue.issue.id, now);
  var task = _iwTask(store, 'ISS01', job.id, { type: 'Issues', id: issue.issue.id }, 'ISS01-' + issue.issue.id, issue.issue.due_at, input, now, 'installer-reported variation on ' + wp.trade);
  _iwCommit(store, input, now);
  return { replay: false, issue: issue.issue, evidence: ev, task: task, approver_id: owner.id, external_calls: 0 };
}

/* --- 5. COMMISSIONING DRAFT / SUBMIT / RESUBMIT (no questions invented; template_version stays NOT_CONFIGURED until approved) --- */

function _iwOpenSubmission(store, wp) { return store.list('CommissioningSubmissions').filter(function (s) { return s.work_package_id === wp.id && ['Draft', 'Returned'].indexOf(s.status) !== -1; }).sort(function (a, b) { return String(b.created_at).localeCompare(String(a.created_at)); })[0] || null; }
function _iwSaveCommissioningDraft(store, input) {
  _iwGuardStore(store); _iwRequirePilot(store);
  var ctx = _iwPackage(store, input.work_package_id), wp = ctx.wp, job = ctx.job, access = _iwAssertAccess(store, input.actor, wp, input);
  var answers = Array.isArray(input.answers) ? input.answers : []; if (!answers.length && !(Array.isArray(input.evidence) && input.evidence.length)) _iwRefuse('IW_REVIEW: answers or evidence required');
  answers.forEach(function (a) { if (!a || !_iwText(a.question_key)) _iwRefuse('IW_REVIEW: question_key required'); });
  var cmd = _iwCommandStart(store, input, 'WorkPackages', wp.id, { action: 'CommissioningDraft', answers: answers.map(function (a) { return [a.question_key, a.value_text === undefined ? null : a.value_text, a.value_number === undefined ? null : a.value_number, a.value_date === undefined ? null : a.value_date, a.value_boolean === undefined ? null : a.value_boolean, a.not_applicable_reason || null]; }) }); if (cmd.replay) return { replay: true };
  var now = _iwNow(input);
  var accepted = store.list('CommissioningSubmissions').filter(function (s) { return s.work_package_id === wp.id && s.status === 'Accepted'; })[0]; if (accepted) _iwRefuse('IW_REVIEW: commissioning already accepted for this package');
  var sub = _iwOpenSubmission(store, wp), created = false;
  if (!sub) {
    var submitted = store.list('CommissioningSubmissions').filter(function (s) { return s.work_package_id === wp.id && ['Submitted', 'UnderReview'].indexOf(s.status) !== -1; })[0]; if (submitted) _iwRefuse('IW_REVIEW: a submission is awaiting review; wait for Returned or Accepted');
    var prior = store.list('CommissioningSubmissions').filter(function (s) { return s.work_package_id === wp.id; }).length;
    var id = prior === 0 ? 'CS-' + wp.id : 'CS-' + wp.id + '-v' + (prior + 1);
    sub = { id: id, job_id: job.id, work_package_id: wp.id, allocation_id: (_iwAllocationsFor(store, wp.id).filter(function (a) { return a.person_id === access.person.id; })[0] || {}).id || null, installer_id: access.via === 'allocation' ? access.person.id : ((_iwAllocationsFor(store, wp.id)[0] || {}).person_id || access.person.id), template_version: input.template_version || 'NOT_CONFIGURED', status: 'Draft', submitted_at: null, reviewed_at: null, reviewed_by: null, review_notes: null, supersedes_submission_id: null, created_at: now, created_by: input.actor, updated_at: now, updated_by: input.actor, version: 1, commit_id: 'IW-' + input.command_id };
    store.insert('CommissioningSubmissions', sub); created = true;
  } else if (sub.status === 'Returned') {
    /* Returned forms are immutable: corrections go into a new superseding draft. */
    var n = store.list('CommissioningSubmissions').filter(function (s) { return s.work_package_id === wp.id; }).length;
    var fresh = Object.assign({}, sub, { id: 'CS-' + wp.id + '-v' + (n + 1), status: 'Draft', submitted_at: null, reviewed_at: null, reviewed_by: null, review_notes: null, supersedes_submission_id: sub.id, created_at: now, created_by: input.actor, updated_at: now, updated_by: input.actor, version: 1, commit_id: 'IW-' + input.command_id });
    store.insert('CommissioningSubmissions', fresh); sub = fresh; created = true;
  }
  var saved = [];
  answers.forEach(function (a) {
    var aid = sub.id + '-A-' + a.question_key, row = { id: aid, submission_id: sub.id, question_key: a.question_key, value_text: a.value_text === undefined ? null : a.value_text, value_number: a.value_number === undefined ? null : a.value_number, value_date: a.value_date === undefined ? null : _iwDate(a.value_date), value_boolean: a.value_boolean === undefined ? null : a.value_boolean, not_applicable_reason: a.not_applicable_reason || null, created_at: now, commit_id: 'IW-' + input.command_id };
    if (store.get('CommissioningAnswers', aid)) store.update('CommissioningAnswers', aid, { value_text: row.value_text, value_number: row.value_number, value_date: row.value_date, value_boolean: row.value_boolean, not_applicable_reason: row.not_applicable_reason });
    else store.insert('CommissioningAnswers', row);
    saved.push(aid);
  });
  var ev = _iwEvidenceRows(store, input, job, wp, input.evidence, 'Commissioning', sub.id, null, now);
  if (!created) store.update('CommissioningSubmissions', sub.id, { updated_at: now, updated_by: input.actor, version: Number(sub.version || 0) + 1 });
  _iwAudit(store, 'CommissioningSubmissions', sub.id, created ? 'DraftCreated' : 'DraftSaved', null, { answers: saved.length, evidence: ev.length }, input, now); _iwCommit(store, input, now);
  return { replay: false, submission_id: sub.id, status: 'Draft', created: created, answers_saved: saved.length, evidence: ev, template_version: sub.template_version, external_calls: 0 };
}
function _iwSubmitCommissioning(store, input) {
  _iwGuardStore(store); _iwRequirePilot(store);
  var ctx = _iwPackage(store, input.work_package_id), wp = ctx.wp; _iwAssertAccess(store, input.actor, wp, input);
  var sub = store.get('CommissioningSubmissions', input.submission_id); if (!sub || sub.work_package_id !== wp.id) _iwRefuse('IW_REVIEW: submission not found for this package');
  var cmd = _iwCommandStart(store, input, 'CommissioningSubmissions', sub.id, { action: 'Submit' }); if (cmd.replay) return { replay: true, status: store.get('CommissioningSubmissions', sub.id).status };
  if (sub.status !== 'Draft') _iwRefuse('IW_REVIEW: only a Draft can be submitted (status ' + sub.status + ')');
  _iwExpect(sub, input);
  var answers = store.list('CommissioningAnswers').filter(function (a) { return a.submission_id === sub.id; }), photos = store.list('Evidence').filter(function (e) { return e.submission_id === sub.id; });
  if (!answers.length && !photos.length) _iwRefuse('IW_REVIEW: nothing to submit (no answers or evidence)');
  var now = _iwNow(input), after = _iwPatch(store, 'CommissioningSubmissions', sub, { status: 'Submitted', submitted_at: now }, input, now);
  _iwAudit(store, 'CommissioningSubmissions', sub.id, 'Submitted', sub, after, input, now); _iwCommit(store, input, now);
  return { replay: false, submission_id: sub.id, status: 'Submitted', answers: answers.length, evidence: photos.length, immutable: true, next: 'Office technical review (S12) — Accepted or Returned', external_calls: 0 };
}


/* Installer workflow cloud adapter: header-checked Sheets store; installer-facing entry points take the signed-in user as actor. */
function _iwCloudConfig() { var c = JSON.parse(PropertiesService.getScriptProperties().getProperty('S01_CONFIG') || 'null'); if (!c || c.environment !== 'DEV') throw new Error('IW_REFUSED: exact DEV environment required'); return c; }
function _iwCloudGuard() { var ss = SpreadsheetApp.getActiveSpreadsheet(); _iwCloudConfig(); if (ss.getId() !== IW_DEV_SHEET_ID) throw new Error('IW_REFUSED: exact DEV sheet/environment required'); return ss; }
function _iwSheet(ss, name) { var m = ss.getSheets().filter(function (s) { return s.getName() === name; }); if (m.length !== 1) throw new Error('IW_SCHEMA: missing/duplicate tab ' + name); var sh = m[0], h = IW_HEADERS[name]; if (!h || sh.getLastColumn() < h.length || JSON.stringify(sh.getRange(1, 1, 1, h.length).getValues()[0]) !== JSON.stringify(h)) throw new Error('IW_SCHEMA: header mismatch ' + name); return sh; }
function _iwCell(v) { return v === null || v === undefined ? '' : typeof v === 'string' && /^[=+@'\-]/.test(v) ? "'" + v : v; }
function _iwCloudStore() {
  var ss = _iwCloudGuard();
  function list(name) { var sh = _iwSheet(ss, name), h = IW_HEADERS[name]; if (sh.getLastRow() < 2) return []; return sh.getRange(2, 1, sh.getLastRow() - 1, h.length).getValues().map(function (row) { var x = {}; h.forEach(function (k, i) { x[k] = row[i] === '' ? null : row[i]; }); return x; }).filter(function (r) { return r.id; }); }
  return {
    getSheetId: function () { return ss.getId(); }, getEnvironment: function () { _iwCloudGuard(); return 'DEV'; }, list: list,
    get: function (name, id) { var rows = list(name).filter(function (r) { return r.id === id; }); if (rows.length > 1) throw new Error('IW_SCHEMA: duplicate ID ' + id); return rows[0] || null; },
    insert: function (name, row) { var sh = _iwSheet(ss, name), h = IW_HEADERS[name]; if (list(name).some(function (r) { return r.id === row.id; })) throw new Error('IW_SCHEMA: duplicate insert ' + row.id); if (sh.getLastRow() >= sh.getMaxRows()) throw new Error('IW_CAPACITY: ' + name); for (var k in row) if (row.hasOwnProperty(k) && h.indexOf(k) === -1) throw new Error('IW_SCHEMA: unknown field ' + name + '.' + k); sh.getRange(sh.getLastRow() + 1, 1, 1, h.length).setValues([h.map(function (k) { return _iwCell(row[k]); })]); SpreadsheetApp.flush(); },
    update: function (name, id, patch) { var sh = _iwSheet(ss, name), h = IW_HEADERS[name], values = sh.getRange(2, 1, Math.max(1, sh.getLastRow() - 1), h.length).getValues(), idx = []; values.forEach(function (r, i) { if (r[0] === id) idx.push(i); }); if (idx.length !== 1) throw new Error('IW_SCHEMA: update row ' + id); var row = values[idx[0]]; for (var k in patch) { if (!patch.hasOwnProperty(k)) continue; if (h.indexOf(k) === -1) throw new Error('IW_SCHEMA: unknown field ' + name + '.' + k); row[h.indexOf(k)] = patch[k]; } sh.getRange(idx[0] + 2, 1, 1, h.length).setValues([row.map(_iwCell)]); SpreadsheetApp.flush(); },
    withLock: function (fn) { var lock = LockService.getScriptLock(); if (!lock.tryLock(5000)) throw new Error('IW_BUSY'); try { return fn(); } finally { lock.releaseLock(); } }
  };
}
function _iwResult(name, fn) { try { var result = fn(); var r = { test: name, pass: result.pass === undefined ? result.ok !== false : result.pass, detail: result.detail || result }; console.log(JSON.stringify(r)); return r; } catch (e) { var f = { test: name, pass: false, detail: String(e.message || e) }; console.log(JSON.stringify(f)); return f; } }
/* The actor is ALWAYS the signed-in user's People row — never a client-supplied id. */
function _iwActorId(store) { var email = ''; try { email = String(Session.getActiveUser().getEmail() || '').trim().toLowerCase(); } catch (e) { email = ''; } if (!email) throw new Error('IW_REFUSED: authenticated user required'); var p = store.list('People').filter(function (x) { return String(x.email || '').toLowerCase() === email && (x.active === true || x.active === 'TRUE'); })[0]; if (!p) throw new Error('IW_REFUSED: signed-in user is not an active People row'); return p.id; }
function _iwStamp() { return new Date().toISOString().replace(/[^0-9]/g, '').substring(0, 14); }
function _iwParse(json) { if (!json) return null; if (typeof json === 'object') return json; try { return JSON.parse(json); } catch (e) { throw new Error('IW_REVIEW: invalid JSON payload'); } }
function _iwSetFn06(store, enable) { return store.withLock(function () { var rows = store.list('ReleaseModes').filter(function (r) { return r.function_id === 'FN-06'; }); if (rows.length !== 1 || rows[0].target_release !== 'R3') throw new Error('IW_REFUSED: FN-06 ReleaseMode invalid'); var r = rows[0], ok = (r.mode === 'Disabled' && r.authorised_job_scope === 'None') || (r.mode === 'Automated' && r.authorised_job_scope === 'Pilot'); if (!ok) throw new Error('IW_REFUSED: FN-06 unexpected state ' + r.mode + '/' + r.authorised_job_scope); store.update('ReleaseModes', r.id, { mode: enable ? 'Automated' : 'Disabled', authorised_job_scope: enable ? 'Pilot' : 'None', updated_at: new Date().toISOString(), updated_by: enable ? 'IW-enable' : 'IW-restore', version: Number(r.version || 0) + 1 }); return { ok: true, enabled: enable, function_id: 'FN-06' }; }); }
function runIwEnableFn06ForSyntheticTest() { return _iwResult('IW enable FN-06', function () { return _iwSetFn06(_iwCloudStore(), true); }); }
function restoreIwSafeState() { return _iwResult('IW restore', function () { return _iwSetFn06(_iwCloudStore(), false); }); }
function runIwMyWork(fromDate, toDate) { return _iwResult('IW my work', function () { var s = _iwCloudStore(); return Object.assign({ ok: true }, _iwMyWork(s, _iwActorId(s), { from: fromDate || null, to: toDate || null })); }); }
/* AppSheet-callable command: payloadJson carries command_id (client-generated for offline duplicate sync) and fields; actor from session. */
function appSheetInstallerCommand(commandType, payloadJson) {
  var s, actor; try { s = _iwCloudStore(); actor = _iwActorId(s); } catch (e) { return JSON.stringify({ ok: false, error: e.message }); }
  var map = { IW_START: _iwStart, IW_PROGRESS: _iwProgress, IW_REPORT_COMPLETION: _iwReportCompletion, IW_REPORT_PROBLEM: _iwReportProblem, IW_REPORT_VARIATION: _iwReportVariation, IW_COMMISSIONING_DRAFT: _iwSaveCommissioningDraft, IW_COMMISSIONING_SUBMIT: _iwSubmitCommissioning };
  var fn = map[commandType]; if (!fn) return JSON.stringify({ ok: false, error: 'IW_UNKNOWN_COMMAND' });
  var p; try { p = _iwParse(payloadJson) || {}; } catch (e) { return JSON.stringify({ ok: false, error: e.message }); }
  p.actor = actor; if (!p.command_id) return JSON.stringify({ ok: false, error: 'IW_REVIEW: command_id required (generate on the device for safe resubmission)' });
  try { return JSON.stringify(Object.assign({ ok: true, command_type: commandType, actor_id: actor }, s.withLock(function () { return fn(s, p); }))); } catch (e) { return JSON.stringify({ ok: false, command_type: commandType, error: e.message }); }
}
/* Synthetic zero-arg smoke on a fresh IW-fixture job with the signed-in user allocated as installer. Restores FN-06. */
function runIwHappyPathTest() {
  return _iwResult('IW happy path', function () {
    var s = _iwCloudStore(), actor = _iwActorId(s);
    _iwSetFn06(s, true);
    try {
      return s.withLock(function () {
        var n = new Date().toISOString(), stamp = _iwStamp(), jobId = 'J-iw-smoke-' + stamp, wpId = 'WP-' + jobId + '-roof', today = n.slice(0, 10);
        s.insert('Jobs', { id: jobId, job_id: 'SS-IW-' + stamp.slice(-6), customer_id: 'CUST-iw', display_name: 'IW Smoke ' + stamp, finance_route: 'Standard', contract_status: 'Signed', sold_booking_match_status: 'Match', roof_required: true, electrical_required: false, scaffold_required: false, workflow_stage: 'InProgress', handover_status: 'NotReady', financial_status: 'Pending', pilot_job: true, release_scope: 'R3', created_at: n, created_by: 'IW-fixture', updated_at: n, updated_by: 'IW-fixture', version: 1, source_system: 'IW-fixture', commit_id: 'IW-fixture' });
        s.insert('WorkPackages', { id: wpId, job_id: jobId, trade: 'Roof', required: true, planned_start: today, planned_end: today, status: 'Scheduled', commissioning_required: true, sequence: 1, revision: 1, created_at: n, created_by: 'IW-fixture', updated_at: n, updated_by: 'IW-fixture', version: 1, source_system: 'IW-fixture', commit_id: 'IW-fixture' });
        s.insert('Allocations', { id: 'ALLOC-' + wpId, work_package_id: wpId, person_id: actor, role: 'Lead', start_at: today, end_at: today, active: true, created_at: n, created_by: 'IW-fixture', updated_at: n, updated_by: 'IW-fixture', version: 1, source_system: 'IW-fixture', commit_id: 'IW-fixture' });
        var mine = _iwMyWork(s, actor, {}), wp = s.get('WorkPackages', wpId);
        var st = _iwStart(s, { actor: actor, command_id: 'IW-' + stamp + '-START', work_package_id: wpId, expected_version: wp.version });
        var pr = _iwProgress(s, { actor: actor, command_id: 'IW-' + stamp + '-PROG', work_package_id: wpId, note: 'Half done', evidence: [{ drive_file_id: 'drive-iw-' + stamp + '-1', filename: 'progress.jpg' }] });
        var pb = _iwReportProblem(s, { actor: actor, command_id: 'IW-' + stamp + '-PROB', work_package_id: wpId, category: 'Access', description: 'Synthetic access issue', blocks_completion: false });
        wp = s.get('WorkPackages', wpId);
        var rc = _iwReportCompletion(s, { actor: actor, command_id: 'IW-' + stamp + '-DONE', work_package_id: wpId, outcome: 'Complete', actual_end: today, expected_version: wp.version, evidence: [{ drive_file_id: 'drive-iw-' + stamp + '-2', filename: 'done.jpg' }] });
        var rcAgain = _iwReportCompletion(s, { actor: actor, command_id: 'IW-' + stamp + '-DONE', work_package_id: wpId, outcome: 'Complete', actual_end: today, expected_version: wp.version, evidence: [{ drive_file_id: 'drive-iw-' + stamp + '-2', filename: 'done.jpg' }] });
        var dr = _iwSaveCommissioningDraft(s, { actor: actor, command_id: 'IW-' + stamp + '-DRAFT', work_package_id: wpId, answers: [{ question_key: 'synthetic_check', value_boolean: true }] });
        var sub = s.get('CommissioningSubmissions', dr.submission_id);
        var sm = _iwSubmitCommissioning(s, { actor: actor, command_id: 'IW-' + stamp + '-SUBMIT', work_package_id: wpId, submission_id: sub.id, expected_version: sub.version });
        var pass = mine.count >= 1 && st.status === 'InProgress' && pr.evidence.length === 1 && pb.issue.status === 'Open' && rc.status === 'ReportedComplete' && rcAgain.replay === true && rc.commissioning_submission && sm.status === 'Submitted' && s.get('WorkPackages', wpId).actual_end === today;
        return { pass: pass, detail: { job_id: jobId, my_work: mine.count, statuses: [st.status, rc.status, sm.status], replay: rcAgain.replay, issue: pb.issue.id, submission: sm.submission_id, template_version: dr.template_version, external_calls: 0 } };
      });
    } finally { _iwSetFn06(s, false); }
  });
}
