/* GENERATED by node scripts/build-s04.cjs — do not edit. */
var S04 = (function() {
// Source: processor/types.js
const m0 = (function() {
/* S03 processor — type definitions, constants, and enums.
 * Authority: 01 §3, §4; RA01 §9. */

const CommandType = Object.freeze({
  COMPLETE_TASK: 'COMPLETE_TASK',
  RECORD_CALL: 'RECORD_CALL',
  RAISE_ISSUE: 'RAISE_ISSUE',
  MOVE_JOB: 'MOVE_JOB',
  CHANGE_INSTALLER: 'CHANGE_INSTALLER',
  CANCEL_JOB: 'CANCEL_JOB',
  APPROVE_OPERATIONAL_COMPLETE: 'APPROVE_OPERATIONAL_COMPLETE',
  CREATE_ORDER: 'CREATE_ORDER',
  RECEIVE_DELIVERY: 'RECEIVE_DELIVERY',
  STOCK_MOVEMENT: 'STOCK_MOVEMENT',
  SUBMIT_COMMISSIONING: 'SUBMIT_COMMISSIONING',
  REVIEW_COMMISSIONING: 'REVIEW_COMMISSIONING',
  SEND_COMMUNICATION: 'SEND_COMMUNICATION',
  RECONCILE: 'RECONCILE',
  HEALTH_CHECK: 'HEALTH_CHECK',
  PROCESS_INTAKE: 'PROCESS_INTAKE'
});

const ReleaseMode = Object.freeze({
  DISABLED: 'Disabled',
  MANUAL: 'Manual',
  AUTOMATED: 'Automated'
});

const CommitState = Object.freeze({
  PREPARED: 'Prepared',
  APPLYING: 'Applying',
  COMMITTED: 'Committed',
  RECOVERY_REQUIRED: 'RecoveryRequired'
});

const OutboxStatus = Object.freeze({
  PENDING: 'Pending',
  PROCESSING: 'Processing',
  SUCCEEDED: 'Succeeded',
  RETRY_DUE: 'RetryDue',
  NEEDS_REVIEW: 'NeedsReview',
  CANCELLED: 'Cancelled'
});

const TaskStatus = Object.freeze({
  BLOCKED: 'Blocked',
  OPEN: 'Open',
  IN_PROGRESS: 'InProgress',
  WAITING: 'Waiting',
  COMPLETE: 'Complete',
  CANCELLED: 'Cancelled',
  NOT_REQUIRED: 'NotRequired'
});

const JobStage = Object.freeze({
  PREBOOKING: 'Prebooking',
  READY_TO_BOOK: 'ReadyToBook',
  BOOKING_IN_PROGRESS: 'BookingInProgress',
  BOOKED: 'Booked',
  AWAITING_INSTALLATION: 'AwaitingInstallation',
  IN_PROGRESS: 'InProgress',
  AFTERCARE: 'Aftercare',
  OPERATIONALLY_COMPLETE: 'OperationallyComplete',
  CANCELLATION_IN_PROGRESS: 'CancellationInProgress',
  CANCELLED: 'Cancelled'
});

const ActorRole = Object.freeze({
  ADMIN: 'Admin',
  MANAGER: 'Manager',
  OFFICE: 'Office',
  FINANCE: 'Finance',
  STORE: 'Store',
  INSTALLER: 'Installer',
  SCAFFOLDER: 'Scaffolder',
  READ_ONLY: 'ReadOnly'
});

const FailureCategory = Object.freeze({
  VALIDATION: 'Validation',
  PERMISSION: 'Permission',
  CONCURRENCY: 'Concurrency',
  DISABLED_MODE: 'DisabledMode',
  PILOT_SCOPE: 'PilotScope',
  TRANSIENT: 'Transient',
  AUTHENTICATION: 'Authentication',
  UNCERTAIN: 'Uncertain',
  FATAL: 'Fatal'
});

function makeCommand(commandType, actor, entityId, expectedVersion, payload) {
  const ts = Date.now();
  const rawPayload = JSON.stringify(payload || {});
  return Object.freeze({
    command_id: 'CMD-' + ts.toString(36) + '-' + Math.random().toString(36).slice(2, 10),
    source: 'appsheet',
    actor_email: actor.email,
    actor_roles: [...actor.roles],
    entity_type: 'Job',
    entity_id: entityId,
    expected_version: expectedVersion || null,
    command_type: commandType,
    payload: Object.freeze({ ...payload }),
    submitted_at: new Date(ts).toISOString(),
    payload_hash: simpleHash(rawPayload)
  });
}

function simpleHash(str) {
  let hash = 0;
  for (let i = 0; i < str.length; i++) {
    const char = str.charCodeAt(i);
    hash = ((hash << 5) - hash) + char;
    hash |= 0;
  }
  return 'h' + (hash >>> 0).toString(16).padStart(8, '0');
}

function makeActor(email, roles, personId) {
  return Object.freeze({
    email,
    roles: Object.freeze([...roles]),
    person_id: personId || null
  });
}

return {
    CommandType, ReleaseMode, CommitState, OutboxStatus, TaskStatus,
    JobStage, ActorRole, FailureCategory,
    makeCommand, makeActor, simpleHash
  };
})();
// Source: processor/journal.js
const m1 = (function() {
/* S03 processor — CommitJournal for deterministic recovery.
 * Authority: 01 §3 command processing, concurrency and partial failure. */

const { CommitState } = m0;

function createJournal() {
  const entries = new Map();
  return { entries };
}

function prepareCommit(journal, commitId, commandId, entityType, entityId, expectedVersion, changesJson) {
  if (journal.entries.has(commitId)) {
    return { ok: false, reason: 'commit_id already exists: ' + commitId };
  }

  const entry = {
    commit_id: commitId,
    state: CommitState.PREPARED,
    command_id: commandId,
    entity_type: entityType,
    entity_id: entityId,
    expected_version: expectedVersion,
    changes_json: JSON.stringify(changesJson),
    prepared_at: new Date().toISOString(),
    committed_at: null
  };

  journal.entries.set(commitId, entry);
  return { ok: true, entry };
}

function markApplying(journal, commitId) {
  const entry = journal.entries.get(commitId);
  if (!entry) return { ok: false, reason: 'commit_id not found: ' + commitId };
  if (entry.state !== CommitState.PREPARED) {
    return { ok: false, reason: 'cannot apply from state ' + entry.state };
  }
  entry.state = CommitState.APPLYING;
  return { ok: true, entry };
}

function markCommitted(journal, commitId) {
  const entry = journal.entries.get(commitId);
  if (!entry) return { ok: false, reason: 'commit_id not found: ' + commitId };
  if (entry.state !== CommitState.APPLYING) {
    return { ok: false, reason: 'cannot commit from state ' + entry.state };
  }
  entry.state = CommitState.COMMITTED;
  entry.committed_at = new Date().toISOString();
  return { ok: true, entry };
}

function markRecoveryRequired(journal, commitId) {
  const entry = journal.entries.get(commitId);
  if (!entry) return { ok: false, reason: 'commit_id not found: ' + commitId };
  entry.state = CommitState.RECOVERY_REQUIRED;
  return { ok: true, entry };
}

function getUncommitted(journal) {
  const uncommitted = [];
  for (const [id, entry] of journal.entries) {
    if (entry.state === CommitState.PREPARED || entry.state === CommitState.APPLYING || entry.state === CommitState.RECOVERY_REQUIRED) {
      uncommitted.push(entry);
    }
  }
  return uncommitted;
}

function getByCommitId(journal, commitId) {
  return journal.entries.get(commitId) || null;
}

return {
    createJournal, prepareCommit, markApplying, markCommitted,
    markRecoveryRequired, getUncommitted, getByCommitId
  };
})();
// Source: processor/tasks.js
const m2 = (function() {
/* S03 processor — Task creation, deduplication, and TaskEvents.
 * Authority: 01 §6 task rules; §4 Tasks, TaskEvents. */

const { TaskStatus } = m0;

function createTaskStore() {
  const tasks = new Map();
  const taskEvents = [];
  return { tasks, taskEvents };
}

function generateTaskId() {
  return 'TASK-' + Date.now().toString(36) + '-' + Math.random().toString(36).slice(2, 8);
}

function createTask(taskStore, template, jobId, ownerId, dueAt, relatedEntity, clock) {
  // Build instance_key for deduplication
  const instanceKey = template.template_code + '-' + (jobId || 'SYS') + '-' +
    (relatedEntity ? relatedEntity.type + '-' + relatedEntity.id : 'ROOT') + '-' +
    (dueAt || 'nodue');

  // Check for existing task with same instance_key
  for (const [, existing] of taskStore.tasks) {
    if (existing.instance_key === instanceKey &&
        existing.status !== TaskStatus.CANCELLED &&
        existing.status !== TaskStatus.COMPLETE) {
      return { created: false, reason: 'duplicate instance_key: ' + instanceKey, existing };
    }
  }

  const now = clock ? clock.nowISO() : new Date().toISOString();
  const task = {
    id: generateTaskId(),
    job_id: jobId || null,
    template_code: template.template_code,
    instance_key: instanceKey,
    group: template.group,
    title: template.title,
    owner_id: ownerId || null,
    backup_id: null,
    related_entity_type: relatedEntity ? relatedEntity.type : null,
    related_entity_id: relatedEntity ? relatedEntity.id : null,
    due_at: dueAt || null,
    original_due_at: dueAt || null,
    priority: 0,
    status: TaskStatus.OPEN,
    blocking_reason: null,
    next_followup_at: null,
    completed_at: null,
    completed_by: null,
    completion_note: null,
    evidence_id: null,
    revision_required: false,
    created_rule_version: template.template_version || '1.0',
    created_at: now,
    created_by: 'processor',
    updated_at: now,
    updated_by: 'processor',
    version: 1,
    source_system: 'processor',
    commit_id: null
  };

  taskStore.tasks.set(task.id, task);

  recordTaskEvent(taskStore, task.id, 'CREATED', null, task.status, null, task.owner_id, null, task.due_at, 'processor');

  return { created: true, task };
}

function completeTask(taskStore, taskId, actor, note, clock) {
  const task = taskStore.tasks.get(taskId);
  if (!task) return { ok: false, reason: 'task not found: ' + taskId };
  if (task.status === TaskStatus.COMPLETE) return { ok: false, reason: 'task already complete' };
  if (task.status === TaskStatus.CANCELLED) return { ok: false, reason: 'task is cancelled' };

  const oldStatus = task.status;
  const now = clock ? clock.nowISO() : new Date().toISOString();

  task.status = TaskStatus.COMPLETE;
  task.completed_at = now;
  task.completed_by = actor;
  task.completion_note = note || null;
  task.updated_at = now;
  task.updated_by = actor;
  task.version++;

  recordTaskEvent(taskStore, taskId, 'COMPLETED', oldStatus, task.status, null, null, null, null, actor);
  return { ok: true, task };
}

function recordTaskEvent(taskStore, taskId, action, oldStatus, newStatus, oldOwner, newOwner, oldDue, newDue, actor) {
  const event = {
    id: 'TEVT-' + Date.now().toString(36) + '-' + Math.random().toString(36).slice(2, 8),
    task_id: taskId,
    action,
    old_status: oldStatus,
    new_status: newStatus,
    old_owner: oldOwner,
    new_owner: newOwner,
    old_due: oldDue,
    new_due: newDue,
    reason: null,
    actor,
    timestamp: new Date().toISOString(),
    created_at: new Date().toISOString(),
    commit_id: null
  };
  taskStore.taskEvents.push(event);
  return event;
}

function getTaskByInstanceKey(taskStore, instanceKey) {
  for (const [, task] of taskStore.tasks) {
    if (task.instance_key === instanceKey) return task;
  }
  return null;
}

return { createTaskStore, createTask, completeTask, recordTaskEvent, getTaskByInstanceKey, generateTaskId };
})();
// Source: processor/reconciler.js
const m3 = (function() {
/* S03 processor — Recovery and reconciliation primitives.
 * Authority: 01 §3 command processing, concurrency and partial failure. */

const { CommitState } = m0;

function createReconciler() {
  return {};
}

function recoverUncommitted(journal, dataStore, auditLog) {
  const uncommitted = [];
  for (const [, entry] of journal.entries) {
    if (entry.state === CommitState.PREPARED) {
      uncommitted.push({ commit_id: entry.commit_id, action: 'discard', reason: 'PREPARED but never applied' });
    } else if (entry.state === CommitState.APPLYING) {
      const changes = JSON.parse(entry.changes_json);
      const appStatus = checkApplicationStatus(dataStore, entry.entity_type, entry.entity_id, changes);

      if (appStatus === 'FULLY_APPLIED') {
        uncommitted.push({ commit_id: entry.commit_id, action: 'commit', reason: 'all changes verified in data store' });
      } else if (appStatus === 'NOT_APPLIED') {
        uncommitted.push({ commit_id: entry.commit_id, action: 'discard', reason: 'no changes found in data store' });
      } else {
        uncommitted.push({ commit_id: entry.commit_id, action: 'manual_review', reason: 'PARTIALLY_APPLIED — some changes present, some missing. Do not assume completion.' });
      }
    } else if (entry.state === CommitState.RECOVERY_REQUIRED) {
      uncommitted.push({ commit_id: entry.commit_id, action: 'manual_review', reason: 'explicitly marked RECOVERY_REQUIRED' });
    }
  }
  return uncommitted;
}

function checkApplicationStatus(dataStore, entityType, entityId, changes) {
  if (!dataStore || !dataStore[entityType]) return 'NOT_APPLIED';
  const record = dataStore[entityType].get(entityId);
  if (!record) return 'NOT_APPLIED';

  const changeKeys = Object.keys(changes).filter(k => !k.startsWith('_'));
  if (changeKeys.length === 0) return 'FULLY_APPLIED';

  let appliedCount = 0;
  for (const [field, newValue] of Object.entries(changes)) {
    if (field.startsWith('_')) continue;
    if (record[field] !== undefined && record[field] === newValue) {
      appliedCount++;
    }
  }

  if (appliedCount === changeKeys.length) return 'FULLY_APPLIED';
  if (appliedCount === 0) return 'NOT_APPLIED';
  return 'PARTIALLY_APPLIED';
}

function reconcileOutbox(outbox, externalStateChecker) {
  const actions = [];
  for (const [, item] of outbox.items) {
    if (item.status === 'NeedsReview') {
      if (externalStateChecker && item.external_id) {
        const externalState = externalStateChecker(item.external_id, item.action_type);
        if (externalState === 'SUCCEEDED') {
          actions.push({ outbox_id: item.id, action: 'mark_succeeded', reason: 'external confirmation' });
        } else if (externalState === 'FAILED') {
          actions.push({ outbox_id: item.id, action: 'mark_failed', reason: 'external failure confirmed' });
        }
      }
    }
  }
  return actions;
}

return { createReconciler, recoverUncommitted, reconcileOutbox, checkApplicationStatus };
})();
// Source: s04/processor.js
const m4 = (function() {
/* Authoritative S04-only processor. Dependencies are injected server adapters,
 * never command fields. No generic S03 command endpoint is exported to cloud. */
const Journal = m1;
const { completeTask } = m2;
const { checkApplicationStatus } = m3;

const DEV_SHEET_ID = '1z7PNZtDdC4Z5eLbmTuQdqp0QpJSmuEvx3QvN3VyNTsc';
const clone = value => JSON.parse(JSON.stringify(value));
function refuse(code) { const e = new Error(code); e.code = code; throw e; }
function canonical(value) {
  if (value === null || typeof value !== 'object') return JSON.stringify(value);
  if (Array.isArray(value)) return '[' + value.map(canonical).join(',') + ']';
  return '{' + Object.keys(value).sort().map(k => JSON.stringify(k) + ':' + canonical(value[k])).join(',') + '}';
}
function keys(value, allowed) {
  if (!value || typeof value !== 'object' || Array.isArray(value) ||
      Object.keys(value).some(k => !allowed.includes(k))) refuse('INVALID_FIELDS');
}
function commandRequest(input) {
  keys(input, ['command_id', 'action', 'task_id', 'expected_version', 'payload', 'payload_hash']);
  for (const name of ['command_id', 'task_id']) {
    if (typeof input[name] !== 'string' || !/^[A-Za-z0-9][A-Za-z0-9_-]{0,119}$/.test(input[name])) refuse('INVALID_' + name.toUpperCase());
  }
  if (input.action !== 'COMPLETE_TASK') refuse('ACTION_DENIED');
  if (!Number.isSafeInteger(input.expected_version) || input.expected_version < 1 || input.expected_version >= Number.MAX_SAFE_INTEGER) refuse('INVALID_VERSION');
  keys(input.payload, ['completion_note']);
  if (typeof input.payload.completion_note !== 'string' || input.payload.completion_note.length > 2000) refuse('INVALID_PAYLOAD');
  return { command_id: input.command_id, action: input.action, task_id: input.task_id,
    expected_version: input.expected_version, payload: { completion_note: input.payload.completion_note } };
}
function active(value) { return value === true; }
function one(rows, code) { if (rows.length !== 1) refuse(code); return rows[0]; }
function guard(options) {
  const c = options.config;
  if (!c || c.environment !== 'DEV') refuse('DEV_ONLY');
  if (c.sheetId !== DEV_SHEET_ID || options.store.getSheetId() !== c.sheetId) refuse('SHEET_ID_MISMATCH');
  if (!c.projectId || options.projectId() !== c.projectId) refuse('PROJECT_ID_MISMATCH');
}
function resolveActor(store, identity) {
  if (!identity || typeof identity.email !== 'string' || !identity.email.trim()) refuse('TRUSTED_IDENTITY_REQUIRED');
  const email = identity.email.trim().toLowerCase();
  const person = one(store.list('People').filter(p => typeof p.email === 'string' && p.email.trim().toLowerCase() === email), 'UNKNOWN_OR_DUPLICATE_ACTOR');
  if (!active(person.active)) refuse('INACTIVE_ACTOR');
  const roles = store.list('PersonRoles').filter(r => r.person_id === person.id && active(r.active)).map(r => r.role);
  if (!roles.length) refuse('NO_ACTIVE_ROLE');
  return { id: person.id, email, roles };
}
function permission(store, actor, task) {
  const rules = store.list('PermissionRules').filter(r => actor.roles.includes(r.role) &&
    ['CompleteTask', '*'].includes(r.action) && ['Tasks', '*'].includes(r.entity));
  let allowed = false;
  for (const rule of rules) {
    if (!['All', 'Assigned'].includes(rule.scope) || typeof rule.allowed !== 'boolean') refuse('INVALID_PERMISSION_RULE');
    const applies = rule.scope === 'All' || task.owner_id === actor.id || task.backup_id === actor.id;
    if (applies && !rule.allowed) refuse('PERMISSION_DENIED');
    if (applies && rule.allowed) allowed = true;
  }
  if (!allowed) refuse('PERMISSION_DENIED');
}
function gates(store, actor, task) {
  if (!task) refuse('TASK_NOT_FOUND');
  permission(store, actor, task);
  const mode = one(store.list('ReleaseModes').filter(r => r.function_id === 'FN-01'), 'MODE_MISSING_OR_DUPLICATE');
  if (mode.mode !== 'Automated') refuse('MODE_DENIED');
  if (mode.authorised_job_scope !== 'Pilot' || mode.target_release !== 'R1') refuse('PILOT_SCOPE_DENIED');
  const job = store.get('Jobs', task.job_id);
  if (!job) refuse('JOB_NOT_FOUND');
  if (!active(job.pilot_job) || job.release_scope !== 'R1') refuse('OUTSIDE_PILOT');
  // Explicit synthetic fixture boundary prevents FN-01 enabling real office tasks.
  if (task.template_code !== 'S04-DEV-COMPLETE' || task.source_system !== 'S04-synthetic' || job.source_system !== 'S04-synthetic') refuse('SYNTHETIC_SCOPE_REQUIRED');
}
function completable(store, task, request) {
  if (task.version !== request.expected_version) refuse('STALE_VERSION');
  if (!['Open', 'InProgress', 'Waiting'].includes(task.status)) refuse('TASK_STATE_DENIED');
  if (task.blocking_reason || task.revision_required !== false) refuse('TASK_BLOCKED');
  if (store.list('TaskDependencies').some(d => d.task_id === task.id && !d.satisfied_at)) refuse('TASK_DEPENDENCY_UNSATISFIED');
}
function persistJournal(store, row, plan) {
  row.changes_json = JSON.stringify(plan);
  if (row.changes_json.length > 45000) refuse('JOURNAL_TOO_LARGE');
  store.update('CommitJournal', row.id, row);
}
function transition(store, row, plan, action, now) {
  const journal = { entries: new Map([[row.commit_id, row]]) };
  const outcome = Journal[action](journal, row.commit_id);
  if (!outcome.ok) refuse('JOURNAL_TRANSITION_INVALID');
  if (action === 'markCommitted') row.committed_at = now();
  persistJournal(store, row, plan);
}
function same(a, b) { return canonical(a) === canonical(b); }
function classify(store, plan) {
  const actualTask = store.get('Tasks', plan.before.id);
  const event = store.get('TaskEvents', plan.task_event.id);
  const audit = store.get('AuditEvents', plan.audit_event.id);
  // Reuse S03 field classification, strengthened with exact full-row and event checks.
  const taskClass = checkApplicationStatus({ Tasks: new Map([[plan.before.id, actualTask]]) }, 'Tasks', plan.before.id, plan.after);
  if (taskClass === 'FULLY_APPLIED' && same(actualTask, plan.after) && same(event, plan.task_event) && same(audit, plan.audit_event)) return 'FULLY_APPLIED';
  if (same(actualTask, plan.before) && !event && !audit) return 'NOT_APPLIED';
  return 'PARTIALLY_APPLIED';
}
function createProcessor(options) {
  function process(input) {
    let locked = false, started = false;
    try {
      guard(options);
      const request = commandRequest(input);
      // Identity provider is installed server-side and verifies transport proof.
      const identity = options.actorProvider.getIdentity(request);
      resolveActor(options.store, identity);
      if (!options.lock.acquire()) refuse('LOCK_BUSY');
      locked = true;
      guard(options);
      const store = options.store;
      const actor = resolveActor(store, identity); // Re-resolve permissions/roles under lock.
      const task = store.get('Tasks', request.task_id); // Never rely on a pre-lock version.
      gates(store, actor, task);
      const canonicalRequest = canonical({ request, actor: { id: actor.id, email: actor.email } });
      const hash = options.sha256(canonicalRequest);
      const rows = store.list('CommitJournal');
      const matches = rows.filter(r => r.command_id === request.command_id);
      if (matches.length > 1) refuse('DUPLICATE_COMMAND_JOURNAL');
      let row = matches[0], plan;
      if (row) {
        plan = JSON.parse(row.changes_json);
        if (plan.contract !== 'S04-1' || plan.canonical_request !== canonicalRequest || plan.request_hash !== hash) refuse('COMMAND_ID_CONFLICT');
        if (row.state === 'Committed' && plan.result) return clone(plan.result);
        if (!['Prepared', 'Applying'].includes(row.state)) refuse('RECOVERY_REQUIRED');
        const classification = classify(store, plan);
        plan.recovery_classification = classification;
        if (classification === 'FULLY_APPLIED') {
          started = true;
          plan.result = plan.intended_result;
          if (row.state === 'Prepared') transition(store, row, plan, 'markApplying');
          transition(store, row, plan, 'markCommitted', options.now);
          return clone(plan.result);
        }
        if (classification === 'PARTIALLY_APPLIED') {
          transition(store, row, plan, 'markRecoveryRequired');
          refuse('RECOVERY_REQUIRED');
        }
      }
      // Any unresolved older command on the target prevents another command applying.
      if (rows.some(r => r.entity_type === 'Tasks' && r.entity_id === task.id && r.command_id !== request.command_id && r.state !== 'Committed')) refuse('RECOVERY_REQUIRED');
      completable(store, task, request);
      if (!row) {
        const time = options.now();
        const commitId = 'S04-' + options.sha256(request.command_id);
        const isolated = { tasks: new Map([[task.id, clone(task)]]), taskEvents: [] };
        const completion = completeTask(isolated, task.id, actor.id, request.payload.completion_note, { nowISO: () => time });
        if (!completion.ok) refuse('TASK_STATE_DENIED');
        const after = completion.task;
        after.commit_id = commitId;
        const taskEvent = isolated.taskEvents[0];
        Object.assign(taskEvent, { id: 'TE-' + commitId, actor: actor.id, timestamp: time, created_at: time, reason: request.payload.completion_note || null, commit_id: commitId });
        const auditEvent = { id: 'AE-' + commitId, entity_type: 'Tasks', entity_id: task.id,
          action: 'COMPLETE_TASK', before_json: JSON.stringify(task), after_json: JSON.stringify(after),
          initiating_actor: actor.id, executing_service: 'S04-DEV', timestamp: time,
          correlation_id: request.command_id, reason: request.payload.completion_note || null,
          commit_id: commitId, created_at: time };
        if (store.get('TaskEvents', taskEvent.id) || store.get('AuditEvents', auditEvent.id)) refuse('ORPHAN_EVENT_REQUIRES_REVIEW');
        const intended = { status: 'Committed', accepted: true, committed: true, command_id: request.command_id,
          task_id: task.id, version: after.version, commit_id: commitId, task_event_id: taskEvent.id, audit_event_id: auditEvent.id };
        plan = { contract: 'S04-1', request, actor: { id: actor.id, email: actor.email }, canonical_request: canonicalRequest,
          request_hash: hash, before: task, after, task_event: taskEvent, audit_event: auditEvent, intended_result: intended, result: null };
        const localJournal = Journal.createJournal();
        row = Journal.prepareCommit(localJournal, commitId, request.command_id, 'Tasks', task.id, request.expected_version, plan).entry;
        Object.assign(row, { id: commitId, created_at: time, prepared_at: time });
        if (row.changes_json.length > 45000) refuse('JOURNAL_TOO_LARGE');
        started = true;
        store.insert('CommitJournal', row);
      } else {
        // NOT_APPLIED: old task and absence of both events were verified exactly.
        // Reuse the same durable plan/IDs, after rechecking current gates/version.
        started = true;
        row.state = 'Prepared';
        persistJournal(store, row, plan);
      }
      transition(store, row, plan, 'markApplying');
      store.update('Tasks', task.id, plan.after);
      store.insert('TaskEvents', plan.task_event);
      store.insert('AuditEvents', plan.audit_event);
      plan.result = plan.intended_result;
      transition(store, row, plan, 'markCommitted', options.now); // Result and terminal state in one row write.
      return clone(plan.result);
    } catch (e) {
      return { status: started ? 'Pending' : 'Failed', accepted: false, committed: false,
        error: e.code || (started ? 'INTERRUPTED_RETRY_SAME_COMMAND' : 'PROCESSOR_ERROR') };
    } finally {
      if (locked) options.lock.release();
    }
  }
  return { process };
}
return { createProcessor, commandRequest, canonical, DEV_SHEET_ID, classify };
})();
// Source: s04/identity.js
const m5 = (function() {
/* Trusted issuer must authenticate the initiating Google user independently.
 * This module verifies its HMAC assertion, never an editable actor field.
 * Secrets belong only to the issuer and the isolated DEV backend. */
const { canonical } = m4;
function createSignedActorProvider(options) {
  return { getIdentity(request) {
    const p = options.proof;
    const fail = () => { const e = new Error('TRUSTED_IDENTITY_REQUIRED'); e.code = e.message; throw e; };
    if (!options.secret || options.secret.length < 32 || !p || p.version !== 'S04-IDENTITY-2' || p.purpose !== 'S04_COMPLETE_TASK' ||
        p.subject !== p.email || typeof p.jti !== 'string' || !/^[A-Za-z0-9_-]{16,120}$/.test(p.jti) ||
        typeof p.email !== 'string' || !p.email.trim() || p.audience !== options.audience ||
        !Number.isSafeInteger(p.issued_at) || !Number.isSafeInteger(p.expires_at) ||
        p.expires_at <= p.issued_at || p.expires_at - p.issued_at > 300000 ||
        p.issued_at > options.now() + 30000 || p.expires_at <= options.now() ||
        p.request_hash !== options.sha256(canonical(request))) fail();
    const message = canonical({ version: p.version, purpose: p.purpose, subject: p.subject, jti: p.jti, email: p.email, audience: p.audience,
      issued_at: p.issued_at, expires_at: p.expires_at, request_hash: p.request_hash });
    const expected = options.hmac(message, options.secret);
    if (typeof p.signature !== 'string' || p.signature.length !== expected.length) fail();
    let mismatch = 0;
    for (let i = 0; i < expected.length; i++) mismatch |= expected.charCodeAt(i) ^ p.signature.charCodeAt(i);
    if (mismatch) fail();
    return { email: p.email, subject: p.subject, jti: p.jti };
  } };
}
return { createSignedActorProvider };
})();
// Source: s04/sheet-store.js
const m6 = (function() {
/* Persistent adapter; all lookups enumerate sheets. Never provisions schema.
 * One S04 script project must be the only writer; its ScriptLock spans writes. */
function createSheetStore(ss, schema, flush) {
  const writable = ['Tasks', 'TaskEvents', 'AuditEvents', 'CommitJournal'];
  function table(name) {
    const definition = schema.tables.find(t => t.name === name);
    if (!definition) throw new Error('Unknown table');
    const matches = ss.getSheets().filter(s => s.getName() === name);
    if (matches.length !== 1) throw new Error('Missing/duplicate tab: ' + name);
    const sheet = matches[0];
    const headers = sheet.getRange(1, 1, 1, sheet.getLastColumn()).getValues()[0];
    const expected = definition.columns.map(c => c.name);
    if (headers.length !== expected.length || new Set(headers).size !== headers.length || expected.some(h => !headers.includes(h))) throw new Error('Schema mismatch: ' + name);
    return { sheet, headers, definition };
  }
  function read(name) {
    const t = table(name), ids = new Set();
    const rows = t.sheet.getLastRow() < 2 ? [] : t.sheet.getRange(2, 1, t.sheet.getLastRow() - 1, t.headers.length).getValues();
    return rows.map((values, index) => {
      const record = {};
      t.headers.forEach((h, i) => {
        const column = t.definition.columns.find(c => c.name === h);
        let v = values[i];
        if (v === '' || v === undefined || v === null) v = null;
        else if (column.type === 'BOOLEAN') {
          if (v === 'TRUE') v = true;
          else if (v === 'FALSE') v = false;
          if (typeof v !== 'boolean') throw new Error('Invalid boolean: ' + name + '.' + h);
        } else if (column.type === 'INTEGER') {
          if (typeof v !== 'number' || !Number.isSafeInteger(v)) throw new Error('Invalid integer: ' + name + '.' + h);
        } else if (Object.prototype.toString.call(v) === '[object Date]') {
          v = column.type === 'DATE' ? v.toISOString().slice(0, 10) : v.toISOString();
        } else if (column.type === 'TEXT') v = String(v);
        record[h] = v;
      });
      if (!record.id || ids.has(record.id)) throw new Error('Missing/duplicate id: ' + name);
      ids.add(record.id);
      return { record, row: index + 2 };
    });
  }
  function write(name, record, insert, id) {
    if (!writable.includes(name) || (!insert && ['TaskEvents', 'AuditEvents'].includes(name))) throw new Error('Write forbidden: ' + name);
    if (!record.id || (id && id !== record.id)) throw new Error('Immutable id');
    const t = table(name), rows = read(name), existing = rows.find(r => r.record.id === record.id);
    if (insert ? !!existing : !existing) throw new Error('Insert/update precondition: ' + name);
    if (Object.keys(record).some(k => !t.headers.includes(k))) throw new Error('Unknown write field');
    for (const c of t.definition.columns) {
      const v = record[c.name];
      if (c.required && (v === null || v === undefined || v === '')) throw new Error('Required field: ' + name + '.' + c.name);
      if (v !== null && v !== undefined) {
        if (c.type === 'BOOLEAN' && typeof v !== 'boolean') throw new Error('Boolean required');
        if (c.type === 'INTEGER' && !Number.isSafeInteger(v)) throw new Error('Integer required');
      }
    }
    const target = insert ? t.sheet.getLastRow() + 1 : existing.row;
    if (target > t.sheet.getMaxRows()) throw new Error('ROW_CAPACITY_REQUIRED: ' + name);
    const values = t.headers.map(h => {
      const v = record[h];
      if (v === null || v === undefined) return '';
      // Prevent notes and JSON text being interpreted as spreadsheet formulae.
      return typeof v === 'string' && /^[=']/.test(v) ? "'" + v : v;
    });
    t.sheet.getRange(target, 1, 1, values.length).setValues([values]);
    flush(); // Durable checkpoint before next write or releasing the lock.
  }
  return {
    getSheetId: () => ss.getId(),
    list: name => read(name).map(r => r.record),
    get: (name, id) => read(name).find(r => r.record.id === id)?.record || null,
    insert: (name, record) => write(name, record, true),
    update: (name, id, record) => write(name, record, false, id)
  };
}
return { createSheetStore };
})();
// Source: s04/runtime.js
const m7 = (function() {
/* Server runtime wiring; no identity is inferred from app-owner Session. */
const { createProcessor, DEV_SHEET_ID } = m4;
const { createSignedActorProvider } = m5;
const { createSheetStore } = m6;
function execute(services, schema, commandJson, proofJson) {
  try {
    const properties = services.PropertiesService.getScriptProperties();
    const config = JSON.parse(properties.getProperty('S04_CONFIG') || 'null');
    if (!config || config.environment !== 'DEV') return { status: 'Failed', error: 'DEV_ONLY', committed: false };
    if (config.sheetId !== DEV_SHEET_ID) return { status: 'Failed', error: 'SHEET_ID_MISMATCH', committed: false };
    if (!config.projectId || config.projectId !== services.ScriptApp.getScriptId()) return { status: 'Failed', error: 'PROJECT_ID_MISMATCH', committed: false };
    const input = JSON.parse(commandJson), proof = JSON.parse(proofJson || 'null');
    const hex = bytes => bytes.map(b => ('0' + ((b + 256) % 256).toString(16)).slice(-2)).join('');
    const u = services.Utilities;
    const sha256 = text => hex(u.computeDigest(u.DigestAlgorithm.SHA_256, text, u.Charset.UTF_8));
    const hmac = (text, secret) => hex(u.computeHmacSha256Signature(text, secret, u.Charset.UTF_8));
    const actorProvider = createSignedActorProvider({ proof, secret: properties.getProperty('S04_IDENTITY_SECRET'),
      audience: 'S04:' + config.projectId + ':' + config.sheetId, now: () => Date.now(), sha256, hmac });
    // Verify before opening any workbook as well as inside the processor boundary.
    const { commandRequest } = m4;
    actorProvider.getIdentity(commandRequest(input));
    const ss = services.SpreadsheetApp.openById(config.sheetId);
    const store = createSheetStore(ss, schema, () => services.SpreadsheetApp.flush());
    const lock = services.LockService.getScriptLock();
    return createProcessor({ config, store, projectId: () => services.ScriptApp.getScriptId(),
      actorProvider, sha256, now: () => new Date().toISOString(),
      lock: { acquire: () => lock.tryLock(5000), release: () => lock.releaseLock() }
    }).process(input);
  } catch (e) {
    return { status: 'Failed', committed: false, accepted: false, error: e.code || 'S04_RUNTIME_REFUSED' };
  }
}
return { execute };
})();
const schema = {"tables":[{"name":"People","purpose":"User directory and role assignments","columns":[{"name":"id","type":"TEXT","required":true,"key":true,"notes":"Immutable primary key"},{"name":"email","type":"TEXT","required":true,"unique":true,"notes":"Normalised login; case-insensitive unique"},{"name":"display_name","type":"TEXT","required":true},{"name":"role","type":"TEXT","required":true,"notes":"Admin/Manager/Office/Finance/Store/Installer/Scaffolder/ReadOnly"},{"name":"company_id","type":"TEXT","nullable":true,"notes":"FK Companies; partner link"},{"name":"active","type":"BOOLEAN","required":true},{"name":"calendar_id","type":"TEXT","nullable":true,"notes":"Validated target calendar"},{"name":"notification_email","type":"TEXT","nullable":true,"notes":"Verified address"},{"name":"capacity_per_day","type":"INTEGER","nullable":true,"notes":"Advisory only"},{"name":"available_from","type":"DATE","nullable":true},{"name":"available_to","type":"DATE","nullable":true},{"name":"backup_person_id","type":"TEXT","nullable":true,"notes":"FK People"},{"name":"created_at","type":"TIMESTAMP","required":true},{"name":"created_by","type":"TEXT","required":true},{"name":"updated_at","type":"TIMESTAMP","required":true},{"name":"updated_by","type":"TEXT","required":true},{"name":"version","type":"INTEGER","required":true,"notes":"Positive integer, optimistic locking"},{"name":"source_system","type":"TEXT","required":true},{"name":"source_record_id","type":"TEXT","nullable":true},{"name":"commit_id","type":"TEXT","required":true}]},{"name":"PersonRoles","purpose":"Join table for multi-role people","columns":[{"name":"id","type":"TEXT","required":true,"key":true},{"name":"person_id","type":"TEXT","required":true,"notes":"FK People"},{"name":"role","type":"TEXT","required":true},{"name":"active","type":"BOOLEAN","required":true},{"name":"created_at","type":"TIMESTAMP","required":true},{"name":"created_by","type":"TEXT","required":true},{"name":"updated_at","type":"TIMESTAMP","required":true},{"name":"updated_by","type":"TEXT","required":true},{"name":"version","type":"INTEGER","required":true},{"name":"source_system","type":"TEXT","required":true},{"name":"commit_id","type":"TEXT","required":true}]},{"name":"PermissionRules","purpose":"Role-based access control rules","columns":[{"name":"id","type":"TEXT","required":true,"key":true},{"name":"role","type":"TEXT","required":true},{"name":"action","type":"TEXT","required":true},{"name":"entity","type":"TEXT","required":true},{"name":"scope","type":"TEXT","required":true,"notes":"All/Assigned/OwnCompany/FinanceOnly"},{"name":"allowed","type":"BOOLEAN","required":true},{"name":"created_at","type":"TIMESTAMP","required":true},{"name":"created_by","type":"TEXT","required":true},{"name":"updated_at","type":"TIMESTAMP","required":true},{"name":"updated_by","type":"TEXT","required":true},{"name":"version","type":"INTEGER","required":true},{"name":"source_system","type":"TEXT","required":true},{"name":"commit_id","type":"TEXT","required":true}]},{"name":"Jobs","purpose":"Core job record — one per installation project","columns":[{"name":"id","type":"TEXT","required":true,"key":true,"notes":"Immutable primary key"},{"name":"job_id","type":"TEXT","required":true,"unique":true,"notes":"SS-XXXX-XXXX random reference; generated with collision retry. Use job_id for external display and id for internal FK references."},{"name":"customer_id","type":"TEXT","required":true,"notes":"FK Customers"},{"name":"display_name","type":"TEXT","required":true,"notes":"Surname and postcode, optionally work label"},{"name":"sold_submission_id","type":"TEXT","nullable":true,"notes":"FK Intake"},{"name":"booking_submission_id","type":"TEXT","nullable":true,"notes":"FK Intake"},{"name":"sold_at","type":"TIMESTAMP","nullable":true},{"name":"salesperson_id","type":"TEXT","nullable":true,"notes":"FK People"},{"name":"lead_source","type":"TEXT","nullable":true},{"name":"quote_reference","type":"TEXT","nullable":true,"notes":"TEXT"},{"name":"presale_file_id","type":"TEXT","nullable":true},{"name":"finance_route","type":"TEXT","required":true,"notes":"Standard/Phoenix/OtherReview"},{"name":"contract_status","type":"TEXT","required":true,"notes":"NotSent/Sent/Signed/Cancelled"},{"name":"contract_id","type":"TEXT","nullable":true},{"name":"contract_signed_at","type":"TIMESTAMP","nullable":true},{"name":"contract_evidence_id","type":"TEXT","nullable":true},{"name":"original_net_pence","type":"INTEGER","nullable":true},{"name":"original_vat_pence","type":"INTEGER","nullable":true},{"name":"original_gross_pence","type":"INTEGER","nullable":true},{"name":"approved_change_pence","type":"INTEGER","nullable":true},{"name":"current_contract_gross_pence","type":"INTEGER","nullable":true},{"name":"valuation_basis","type":"TEXT","nullable":true},{"name":"sold_booking_match_status","type":"TEXT","required":true,"notes":"Pending/Match/Review/ApprovedDifference"},{"name":"customer_details_verified_at","type":"TIMESTAMP","nullable":true},{"name":"customer_details_verified_by","type":"TEXT","nullable":true},{"name":"deposit_bank_confirmed_at","type":"TIMESTAMP","nullable":true},{"name":"deposit_bank_confirmed_by","type":"TEXT","nullable":true},{"name":"deposit_bank_reference","type":"TEXT","nullable":true,"notes":"Redacted evidence reference, not bank credentials"},{"name":"roof_required","type":"BOOLEAN","required":true},{"name":"electrical_required","type":"BOOLEAN","required":true},{"name":"scaffold_required","type":"BOOLEAN","required":true},{"name":"workflow_stage","type":"TEXT","required":true,"notes":"Prebooking/ReadyToBook/BookingInProgress/Booked/AwaitingInstallation/InProgress/Aftercare/OperationallyComplete/CancellationInProgress/Cancelled"},{"name":"booking_approved_at","type":"TIMESTAMP","nullable":true},{"name":"booking_approved_by","type":"TEXT","nullable":true},{"name":"operational_complete_at","type":"TIMESTAMP","nullable":true},{"name":"operational_complete_by","type":"TEXT","nullable":true},{"name":"customer_happy_at","type":"TIMESTAMP","nullable":true},{"name":"customer_happy_by","type":"TEXT","nullable":true},{"name":"handover_status","type":"TEXT","required":true,"notes":"NotReady/Ready/Generating/Review/Approved/Sent"},{"name":"financial_status","type":"TEXT","required":true,"notes":"Derived from InvoiceStages"},{"name":"cancellation_at","type":"TIMESTAMP","nullable":true},{"name":"cancellation_by","type":"TEXT","nullable":true},{"name":"cancellation_reason","type":"TEXT","nullable":true},{"name":"archived_at","type":"TIMESTAMP","nullable":true},{"name":"next_action_at","type":"TIMESTAMP","nullable":true,"notes":"Derived from earliest pending task/event"},{"name":"account_policy_version","type":"TEXT","nullable":true},{"name":"pilot_job","type":"BOOLEAN","required":true,"notes":"RA01: pilot scope tracking"},{"name":"release_scope","type":"TEXT","required":true,"notes":"RA01: R1/R2/R3/R4 — which release enrolled this job"},{"name":"created_at","type":"TIMESTAMP","required":true},{"name":"created_by","type":"TEXT","required":true},{"name":"updated_at","type":"TIMESTAMP","required":true},{"name":"updated_by","type":"TEXT","required":true},{"name":"version","type":"INTEGER","required":true},{"name":"source_system","type":"TEXT","required":true},{"name":"source_record_id","type":"TEXT","nullable":true},{"name":"commit_id","type":"TEXT","required":true}]},{"name":"Tasks","purpose":"Work items generated from templates or manually","columns":[{"name":"id","type":"TEXT","required":true,"key":true},{"name":"job_id","type":"TEXT","nullable":true,"notes":"FK Jobs; null for daily system tasks"},{"name":"template_code","type":"TEXT","required":true,"notes":"PRE01–SYS02 etc."},{"name":"instance_key","type":"TEXT","required":true,"unique":true,"notes":"template + entity + revision/episode; prevents duplicates"},{"name":"group","type":"TEXT","required":true,"notes":"Prebooking/Booking/Materials/Install/Aftercare/Finance/Cancellation/System"},{"name":"title","type":"TEXT","required":true},{"name":"owner_id","type":"TEXT","required":true,"notes":"FK People"},{"name":"backup_id","type":"TEXT","nullable":true,"notes":"FK People"},{"name":"related_entity_type","type":"TEXT","nullable":true},{"name":"related_entity_id","type":"TEXT","nullable":true},{"name":"due_at","type":"TIMESTAMP","nullable":true},{"name":"original_due_at","type":"TIMESTAMP","nullable":true},{"name":"priority","type":"INTEGER","required":true},{"name":"status","type":"TEXT","required":true,"notes":"Blocked/Open/InProgress/Waiting/Complete/Cancelled/NotRequired"},{"name":"blocking_reason","type":"TEXT","nullable":true},{"name":"next_followup_at","type":"TIMESTAMP","nullable":true},{"name":"completed_at","type":"TIMESTAMP","nullable":true},{"name":"completed_by","type":"TEXT","nullable":true},{"name":"completion_note","type":"TEXT","nullable":true},{"name":"evidence_id","type":"TEXT","nullable":true},{"name":"revision_required","type":"BOOLEAN","required":true},{"name":"created_rule_version","type":"TEXT","required":true},{"name":"created_at","type":"TIMESTAMP","required":true},{"name":"created_by","type":"TEXT","required":true},{"name":"updated_at","type":"TIMESTAMP","required":true},{"name":"updated_by","type":"TEXT","required":true},{"name":"version","type":"INTEGER","required":true},{"name":"source_system","type":"TEXT","required":true},{"name":"commit_id","type":"TEXT","required":true}]},{"name":"TaskDependencies","purpose":"Prerequisite relationships between tasks","columns":[{"name":"id","type":"TEXT","required":true,"key":true},{"name":"task_id","type":"TEXT","required":true,"notes":"FK Tasks"},{"name":"prerequisite_task_id","type":"TEXT","nullable":true,"notes":"FK Tasks; null for named gate"},{"name":"named_gate","type":"TEXT","nullable":true},{"name":"satisfied_at","type":"TIMESTAMP","nullable":true},{"name":"created_at","type":"TIMESTAMP","required":true},{"name":"commit_id","type":"TEXT","required":true}]},{"name":"TaskEvents","purpose":"Immutable task history log","columns":[{"name":"id","type":"TEXT","required":true,"key":true},{"name":"task_id","type":"TEXT","required":true,"notes":"FK Tasks"},{"name":"action","type":"TEXT","required":true},{"name":"old_status","type":"TEXT","nullable":true},{"name":"new_status","type":"TEXT","nullable":true},{"name":"old_owner","type":"TEXT","nullable":true},{"name":"new_owner","type":"TEXT","nullable":true},{"name":"old_due","type":"TIMESTAMP","nullable":true},{"name":"new_due","type":"TIMESTAMP","nullable":true},{"name":"reason","type":"TEXT","nullable":true},{"name":"actor","type":"TEXT","required":true},{"name":"timestamp","type":"TIMESTAMP","required":true},{"name":"created_at","type":"TIMESTAMP","required":true},{"name":"commit_id","type":"TEXT","required":true}]},{"name":"AuditEvents","purpose":"Immutable audit log","columns":[{"name":"id","type":"TEXT","required":true,"key":true},{"name":"entity_type","type":"TEXT","required":true},{"name":"entity_id","type":"TEXT","required":true},{"name":"action","type":"TEXT","required":true},{"name":"before_json","type":"TEXT","nullable":true},{"name":"after_json","type":"TEXT","nullable":true},{"name":"initiating_actor","type":"TEXT","required":true},{"name":"executing_service","type":"TEXT","required":true},{"name":"timestamp","type":"TIMESTAMP","required":true},{"name":"correlation_id","type":"TEXT","nullable":true},{"name":"reason","type":"TEXT","nullable":true},{"name":"commit_id","type":"TEXT","required":true},{"name":"created_at","type":"TIMESTAMP","required":true}]},{"name":"CommitJournal","purpose":"Write-ahead commit journal for recovery","columns":[{"name":"id","type":"TEXT","required":true,"key":true},{"name":"commit_id","type":"TEXT","required":true,"unique":true},{"name":"state","type":"TEXT","required":true,"notes":"Prepared/Applying/Committed/RecoveryRequired"},{"name":"command_id","type":"TEXT","required":true},{"name":"entity_type","type":"TEXT","required":true},{"name":"entity_id","type":"TEXT","required":true},{"name":"expected_version","type":"INTEGER","nullable":true},{"name":"changes_json","type":"TEXT","required":true},{"name":"prepared_at","type":"TIMESTAMP","required":true},{"name":"committed_at","type":"TIMESTAMP","nullable":true},{"name":"created_at","type":"TIMESTAMP","required":true}]},{"name":"ReleaseModes","purpose":"RA01 per-function release mode tracking","columns":[{"name":"id","type":"TEXT","required":true,"key":true},{"name":"function_id","type":"TEXT","required":true,"unique":true,"notes":"FN-01–FN-20"},{"name":"function_name","type":"TEXT","required":true},{"name":"mode","type":"TEXT","required":true,"notes":"Disabled/Manual/Automated"},{"name":"mode_record_basis","type":"TEXT","required":true},{"name":"authorised_job_scope","type":"TEXT","required":true,"notes":"RA01: pilot/non-pilot boundary"},{"name":"target_release","type":"TEXT","required":true,"notes":"R1/R2/R3/R4"},{"name":"planned_target_mode","type":"TEXT","required":true},{"name":"current_system","type":"TEXT","required":true,"notes":"RA01: existing route description"},{"name":"fallback","type":"TEXT","required":true,"notes":"RA01: manual fallback procedure"},{"name":"external_ids_protected_reference","type":"TEXT","nullable":true},{"name":"activation_time","type":"TIMESTAMP","nullable":true},{"name":"approved_version","type":"TEXT","nullable":true},{"name":"ben_approval_reference","type":"TEXT","nullable":true,"notes":"RA01: Ben's recorded approval per R?-S20"},{"name":"scope_boundary_notes","type":"TEXT","nullable":true},{"name":"created_at","type":"TIMESTAMP","required":true},{"name":"created_by","type":"TEXT","required":true},{"name":"updated_at","type":"TIMESTAMP","required":true},{"name":"updated_by","type":"TEXT","required":true},{"name":"version","type":"INTEGER","required":true},{"name":"commit_id","type":"TEXT","required":true}]}]};
return { createProcessor: m4.createProcessor, commandRequest: m4.commandRequest, canonical: m4.canonical, createSignedActorProvider: m5.createSignedActorProvider, createSheetStore: m6.createSheetStore, execute: function(services, command, proof) { return m7.execute(services, schema, command, proof); } };
})();
