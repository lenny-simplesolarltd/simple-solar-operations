/* Generated by npm run build:s17. DEV synthetic only. */
var S17_HEADERS={"People":["id","email","display_name","role","company_id","active","calendar_id","notification_email","capacity_per_day","available_from","available_to","backup_person_id","created_at","created_by","updated_at","updated_by","version","source_system","source_record_id","commit_id"],"PersonRoles":["id","person_id","role","active","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"PermissionRules":["id","role","action","entity","scope","allowed","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Companies":["id","name","type","active","standard_lead_days","delivery_weekday","notes","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Contacts":["id","company_id","name","email","phone","contact_role","active","preferred_channel","verified_at","verified_by","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Jobs":["id","job_id","customer_id","display_name","sold_submission_id","booking_submission_id","sold_at","salesperson_id","lead_source","quote_reference","presale_file_id","finance_route","contract_status","contract_id","contract_signed_at","contract_evidence_id","original_net_pence","original_vat_pence","original_gross_pence","approved_change_pence","current_contract_gross_pence","valuation_basis","sold_booking_match_status","customer_details_verified_at","customer_details_verified_by","deposit_bank_confirmed_at","deposit_bank_confirmed_by","deposit_bank_reference","roof_required","electrical_required","scaffold_required","workflow_stage","booking_approved_at","booking_approved_by","operational_complete_at","operational_complete_by","customer_happy_at","customer_happy_by","handover_status","financial_status","cancellation_at","cancellation_by","cancellation_reason","archived_at","next_action_at","account_policy_version","pilot_job","release_scope","created_at","created_by","updated_at","updated_by","version","source_system","source_record_id","commit_id"],"Customers":["id","first_name","last_name","address_line1","address_line2","town","postcode","email","phone","alternate_contact","contact_notes","created_at","created_by","updated_at","updated_by","version","source_system","source_record_id","commit_id"],"CustomerChanges":["id","job_id","field_name","previous_value","incoming_value","source_submission_id","resolution","resolved_value","resolved_at","resolved_by","reason","created_at","commit_id"],"Intake":["id","intake_id","form_type","form_id","submission_id","source_revision","received_at","raw_payload_json","payload_hash","job_id","processing_status","validation_errors","processed_at","retry_count","created_at","commit_id"],"MappingRules":["id","form_id","question_id","source_label","target_table","target_field","transform","required_when","active","mapping_version","effective_from","owner","disposition","created_at","created_by","updated_at","updated_by","version","commit_id"],"WorkPackages":["id","job_id","trade","required","planned_start","planned_end","actual_start","actual_end","status","need_by_date","completion_outcome","installer_confirmation_at","installer_confirmation_by","commissioning_required","sequence","revision","parent_package_id","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Allocations":["id","work_package_id","person_id","role","start_at","end_at","active","replaced_allocation_id","cancellation_reason","calendar_link_id","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Tasks":["id","job_id","template_code","instance_key","group","title","owner_id","backup_id","related_entity_type","related_entity_id","due_at","original_due_at","priority","status","blocking_reason","next_followup_at","completed_at","completed_by","completion_note","evidence_id","revision_required","created_rule_version","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"TaskDependencies":["id","task_id","prerequisite_task_id","named_gate","satisfied_at","created_at","commit_id"],"TaskEvents":["id","task_id","action","old_status","new_status","old_owner","new_owner","old_due","new_due","reason","actor","timestamp","created_at","commit_id"],"Calls":["id","job_id","work_package_id","task_id","type","contact_id","person_id","attempted_at","attempted_by","outcome","notes","next_attempt_at","actual_completion_confirmed","customer_happy","strip_authorised","created_at","commit_id"],"Issues":["id","job_id","work_package_id","type","category","description","raised_at","raised_by","responsible_person_id","responsible_company_id","office_owner_id","severity","status","due_at","next_followup_at","blocks_completion","blocks_strip","estimated_value_pence","approved_value_pence","approval_status","approved_at","approved_by","resolution","resolved_at","closed_at","closed_by","customer_resolution_confirmed","linked_return_package_id","evidence_folder_id","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"IssueEvents":["id","issue_id","event_type","actor","timestamp","note","previous_status","new_status","evidence_id","created_at","commit_id"],"Products":["id","sku","name","category","wattage","manufacturer","model","unit","unit_precision","stock_tracked","active","default_supplier_id","standard_lead_days","unit_cost_pence","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Materials":["id","job_id","work_package_id","product_id","description","required_quantity","unit","source","need_by_date","merchant_id","order_line_id","already_ordered_reference","notes","revision","cancelled_quantity","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Reservations":["id","material_id","product_id","location_id","quantity","status","picked_quantity","picked_at","picked_by","created_at","created_by","updated_at","updated_by","version","commit_id"],"Orders":["id","job_id","merchant_id","work_type","requested_delivery_date","delivery_location_id","delivery_address","status","revision","supplier_reference","sent_message_id","confirmed_revision","confirmed_at","confirmed_by","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"OrderLines":["id","order_id","material_id","product_id","description_snapshot","quantity","unit","unit_net_cost_pence","vat_code","cancelled_quantity","created_at","commit_id"],"Deliveries":["id","order_id","expected_date","actual_received_at","received_by","delivery_note_reference","receipt_status","discrepancy_note","created_at","commit_id"],"ReceiptLines":["id","delivery_id","order_line_id","quantity_good","quantity_damaged","evidence_id","stock_movement_ids","created_at","commit_id"],"StockLocations":["id","name","type","job_id","usable","created_at","created_by","updated_at","updated_by","version","commit_id"],"StockMovements":["id","product_id","quantity","from_location_id","to_location_id","movement_type","job_id","receipt_line_id","reason","evidence_id","approval_id","movement_at","idempotency_key","created_at","commit_id"],"Stocktakes":["id","location_id","counted_at","cut_off_commit_id","status","counted_by","approved_by","created_at","commit_id"],"StocktakeLines":["id","stocktake_id","product_id","expected_quantity_at_cutoff","counted_quantity","variance","reason","adjustment_movement_id","created_at","commit_id"],"PanelUse":["id","job_id","roof_package_id","product_id","issued_quantity","installed_quantity","unused_quantity","defective_quantity","broken_quantity","photos","roofer_notes","reported_by","reported_at","reviewed_by","reviewed_at","created_at","commit_id"],"ScaffoldBookings":["id","job_id","company_id","erect_planned_at","erect_confirmed_at","erect_actual_at","strip_forecast_at","strip_authorised_at","strip_authorised_by","strip_planned_at","strip_confirmed_at","strip_actual_at","status","revision","confirmed_revision","access_notes","scope_file_id","quoted_cost_pence","actual_cost_pence","invoice_reference","related_issue_ids","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Communications":["id","job_id","company_id","type","subject","body_snapshot","attachment_ids","recipients_snapshot","covered_week_start","delivery_date","revision","status","approved_at","approved_by","sent_at","external_message_id","outbox_id","created_at","created_by","updated_at","updated_by","version","commit_id"],"CommunicationJobs":["id","communication_id","job_id","order_id","scaffold_booking_id","entity_revision","created_at","commit_id"],"Acknowledgements":["id","communication_id","company_id","entity_id","acknowledged_revision","response","response_text","received_at","recorded_by","evidence_id","created_at","commit_id"],"CommissioningTemplates":["id","trade","equipment_type","template_version","effective_from","active","approved_by","approved_at","created_at","created_by","updated_at","updated_by","version","commit_id"],"CommissioningQuestions":["id","template_id","question_key","label","data_type","required_when","allowed_values","photo_category","review_rule","help_text","display_order","created_at","created_by","updated_at","updated_by","version","commit_id"],"CommissioningSubmissions":["id","job_id","work_package_id","allocation_id","installer_id","template_version","status","submitted_at","reviewed_at","reviewed_by","review_notes","supersedes_submission_id","created_at","created_by","updated_at","updated_by","version","commit_id"],"CommissioningAnswers":["id","submission_id","question_key","value_text","value_number","value_date","value_boolean","not_applicable_reason","created_at","commit_id"],"Evidence":["id","job_id","submission_id","issue_id","category","drive_file_id","filename","mime_type","upload_status","captured_at","captured_by","received_at","customer_shareable","version","checksum","created_at","commit_id"],"FinancePlans":["id","job_id","route","agreed_gross_pence","currency","policy_version","vat_basis","deposit_pct","interim_pct","balance_pct","first_installation_date","interim_due_date","operational_earned_date","accounting_recognition_date","finance_agreement_status","finance_provider_id","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"InvoiceStages":["id","job_id","stage","amount_net_pence","vat_pence","gross_pence","due_date","status","xero_invoice_id","invoice_number","xero_contact_id","reference","request_id","last_synced_at","source_status","sent_at","cancelled_at","created_at","created_by","updated_at","updated_by","version","source_system","commit_id"],"Payments":["id","invoice_stage_id","xero_payment_id","amount_pence","payment_date","status","reconciliation_evidence","last_synced_at","created_at","commit_id"],"ManualBankChecks":["id","job_id","stage","checked_at","checked_by","amount_pence","outcome","evidence_reference","created_at","commit_id"],"AccountingEvents":["id","job_id","policy_version","event_type","effective_date","net_amount_pence","debit_account","credit_account","xero_journal_id","status","reviewed_by","reviewed_at","source_invoice_ids","created_at","created_by","updated_at","updated_by","version","commit_id"],"JobCosts":["id","job_id","category","supplier_id","source_document_id","amount_net_pence","vat_pence","status","accounting_date","policy_version","created_at","created_by","updated_at","updated_by","version","commit_id"],"GHLTasks":["id","job_id","task_id","opportunity_id","target_pipeline_id","target_stage_id","template_id","readiness_snapshot","completed_at","completed_by","evidence_reference","created_at","commit_id"],"Handover":["id","job_id","checklist_version","required_document_types","completeness_status","generated_file_id","generated_version","reviewed_at","reviewed_by","approved_at","approved_by","sent_at","communication_id","created_at","created_by","updated_at","updated_by","version","commit_id"],"CalendarLinks":["id","job_id","allocation_id","scaffold_activity_id","calendar_id","external_event_id","event_uid","producer","entity_revision","last_synced_revision","status","start_at","end_at","all_day","guest_person_ids","description_snapshot","last_attempt_at","last_success_at","error","outbox_id","created_at","created_by","updated_at","updated_by","version","commit_id"],"TechnicalDetails":["id","job_id","system_kw","battery_kwh","annual_generation_kwh","annual_consumption_kwh","mpan","fuse_rating_amps","roof_type","mounting_orientation","survey_file_id","roof_design_file_id","schematic_file_id","shutdown_file_id","g99_status","g99_reference","technical_review_at","technical_review_by","roof_notes","electrical_notes","ordering_notes","created_at","created_by","updated_at","updated_by","version","commit_id"],"JobEquipment":["id","job_id","work_package_id","equipment_type","planned_product_id","installed_product_id","quantity","planned_location","installed_location","serial_number","commissioning_submission_id","variation_id","technical_review_status","created_at","created_by","updated_at","updated_by","version","commit_id"],"AuditEvents":["id","entity_type","entity_id","action","before_json","after_json","initiating_actor","executing_service","timestamp","correlation_id","reason","commit_id","created_at"],"CommitJournal":["id","commit_id","state","command_id","entity_type","entity_id","expected_version","changes_json","prepared_at","committed_at","created_at"],"Outbox":["id","idempotency_key","action_type","target","payload_hash","job_revision","attempt_count","next_attempt","external_id","response_summary","correlation_id","status","created_at","commit_id"],"HealthChecks":["id","integration","checked_at","outcome","last_success","error_code","next_action_task_id","created_at","commit_id"],"ArchiveIndex":["id","job_id","archive_location","archived_at","record_counts","checksum","schema_version","restored_at","created_at","commit_id"],"ReportSnapshots":["id","period_start","period_end","as_of_at","policy_version","report_type","totals_json","underlying_job_ids","file_id","generated_by","created_at","commit_id"],"Settings":["id","key","typed_value","scope","version","effective_from","changed_by","reason","created_at","commit_id"],"Holidays":["id","local_date","description","office_closed","created_at","created_by","commit_id"],"TaskTemplates":["id","template_code","title","group","default_owner_role","trigger_event","due_rule","evidence_required","active","template_version","created_at","created_by","updated_at","updated_by","version","commit_id"],"ReleaseModes":["id","function_id","function_name","mode","mode_record_basis","authorised_job_scope","target_release","planned_target_mode","current_system","fallback","external_ids_protected_reference","activation_time","approved_version","ben_approval_reference","scope_boundary_notes","created_at","created_by","updated_at","updated_by","version","commit_id"]};
/* S17 finished screens / admin read models. Authority: 01 §12, 02 §§1,15-18, 04 S17, RA01.
 * Purely read-only over S01–S16 operational data. No new mutations.
 * No dedicated ReleaseMode — S17 spans all functions; respects existing modes.
 * No real Calendar/Xero/GHL/Drive calls. No destructive operations. */
'use strict';

const S17_ADMIN_DEV_SHEET_ID = '1z7PNZtDdC4Z5eLbmTuQdqp0QpJSmuEvx3QvN3VyNTsc';

/* --- Utilities --- */

function _s17Copy(x) { return JSON.parse(JSON.stringify(x)); }
function _s17Text(x) { return typeof x === 'string' && x.trim().length > 0; }
function _s17Date(value) {
  if (value === null || value === undefined || value === '') return null;
  if (Object.prototype.toString.call(value) === '[object Date]') {
    if (isNaN(value.getTime())) throw new Error('S17_DATE_INVALID');
    if (typeof Utilities !== 'undefined' && Utilities.formatDate) return Utilities.formatDate(value, 'Europe/London', 'yyyy-MM-dd');
    return new Intl.DateTimeFormat('en-CA', { timeZone: 'Europe/London', year: 'numeric', month: '2-digit', day: '2-digit' }).format(value);
  }
  var text = String(value).trim();
  var m = /^(\d{4})-(\d{2})-(\d{2})/.exec(text);
  if (!m) throw new Error('S17_DATE_INVALID');
  var iso = m[1] + '-' + m[2] + '-' + m[3];
  var d = new Date(iso + 'T12:00:00Z');
  if (!isNaN(d.getTime()) && d.toISOString().slice(0, 10) === iso) return iso;
  throw new Error('S17_DATE_INVALID');
}
function _s17Now() { return new Date().toISOString(); }
function _s17Today() { return _s17Date(_s17Now().slice(0, 10)); }

/* --- Guard --- */

function _s17GuardStore(store) {
  if (!store.getSheetId || store.getSheetId() !== S17_ADMIN_DEV_SHEET_ID || !store.getEnvironment || store.getEnvironment() !== 'DEV')
    throw new Error('S17_REFUSED: exact DEV sheet/environment required');
}
function _s17Scope(store) {
  _s17GuardStore(store);
  // S17 is read-only — validate environment only, not specific mode
  var modes = {};
  var modeRows = store.list('ReleaseModes');
  for (var i = 0; i < modeRows.length; i++) {
    var r = modeRows[i];
    modes[r.function_id] = { mode: r.mode, scope: r.authorised_job_scope, target_release: r.target_release, name: r.function_name };
  }
  return modes;
}

/* --- 1. OFFICE HOME / TODAY --- */

function _s17OfficeToday(store, input) {
  _s17Scope(store);
  var today = input && input.as_of ? _s17Date(input.as_of) : _s17Today();
  var now = _s17Now();

  var allTasks = store.list('Tasks').filter(function (t) {
    return t.status !== 'Cancelled' && t.status !== 'Complete' && t.status !== 'NotRequired';
  });

  // Overdue: due_at before today
  var overdue = allTasks.filter(function (t) {
    if (!t.due_at) return false;
    var due = _s17Date(t.due_at);
    return due && due < today;
  }).map(_s17TaskSummary);

  // Due today
  var dueToday = allTasks.filter(function (t) {
    if (!t.due_at) return false;
    var due = _s17Date(t.due_at);
    return due === today;
  }).map(_s17TaskSummary);

  // Due soon (next 7 days, excluding today)
  var dueSoon = allTasks.filter(function (t) {
    if (!t.due_at) return false;
    var due = _s17Date(t.due_at);
    if (!due || due <= today) return false;
    var d = new Date(today + 'T12:00:00Z');
    d.setUTCDate(d.getUTCDate() + 7);
    return due <= d.toISOString().slice(0, 10);
  }).map(_s17TaskSummary);

  // Booking approvals
  var bookingReview = allTasks.filter(function (t) {
    return t.group === 'Booking' || t.group === 'Prebooking';
  }).map(_s17TaskSummary);

  // Unresolved issues
  var unresolvedIssues = store.list('Issues').filter(function (i) {
    return !['Resolved', 'Closed'].includes(i.status);
  }).map(function (i) {
    return { id: i.id, job_id: i.job_id, type: i.type, category: i.category, description: i.description, severity: i.severity, status: i.status, raised_at: i.raised_at ? _s17Date(i.raised_at) : null, office_owner_id: i.office_owner_id, blocks_completion: i.blocks_completion, blocks_strip: i.blocks_strip };
  });

  // Health alerts
  var healthAlerts = [];
  var stalledCommits = store.list('CommitJournal').filter(function (j) { return j.state !== 'Committed'; });
  if (stalledCommits.length > 0) healthAlerts.push({ type: 'stalled_commits', count: stalledCommits.length, detail: stalledCommits.length + ' commit(s) not committed' });
  var uncertainOutbox = store.list('Outbox').filter(function (o) { return ['NeedsReview', 'RetryDue'].includes(o.status); });
  if (uncertainOutbox.length > 0) healthAlerts.push({ type: 'uncertain_outbox', count: uncertainOutbox.length, detail: uncertainOutbox.length + ' outbox item(s) need review' });

  return {
    as_of: today,
    generated_at: now,
    overdue_count: overdue.length,
    due_today_count: dueToday.length,
    due_soon_count: dueSoon.length,
    booking_review_count: bookingReview.length,
    unresolved_issues_count: unresolvedIssues.length,
    health_alerts_count: healthAlerts.length,
    overdue: overdue,
    due_today: dueToday,
    due_soon: dueSoon,
    booking_review: bookingReview,
    unresolved_issues: unresolvedIssues,
    health_alerts: healthAlerts
  };
}

function _s17TaskSummary(t) {
  return {
    id: t.id, job_id: t.job_id, title: t.title, group: t.group,
    owner_id: t.owner_id, due_at: t.due_at ? _s17Date(t.due_at) : null,
    status: t.status, priority: t.priority, blocking_reason: t.blocking_reason,
    template_code: t.template_code, related_entity_type: t.related_entity_type, related_entity_id: t.related_entity_id
  };
}

/* --- 2. JOB OVERVIEW --- */

function _s17JobOverview(store, jobId) {
  _s17Scope(store);
  var job = store.get('Jobs', jobId);
  if (!job) return { job_id: jobId, found: false };

  var overview = {
    found: true,
    identity: {
      id: job.id, job_id: job.job_id, display_name: job.display_name,
      customer_id: job.customer_id, workflow_stage: job.workflow_stage,
      pilot_job: job.pilot_job, release_scope: job.release_scope,
      financial_status: job.financial_status, handover_status: job.handover_status
    },
    booking: _s17BookingSummary(store, job),
    work: _s17WorkSummary(store, job),
    materials: _s17MaterialSummary(store, job),
    scaffold: _s17ScaffoldSummary(store, job),
    commissioning: _s17CommissioningSummary(store, job),
    handover: _s17HandoverSummary(store, job),
    finance: _s17FinanceSummary(store, job),
    crm: _s17CRMSummary(store, job),
    cancellation: _s17CancellationSummary(store, job),
    archive: { archived_at: job.archived_at ? _s17Date(job.archived_at) : null },
    system: _s17SystemSummary(store, job)
  };
  return overview;
}

function _s17BookingSummary(store, job) {
  var tasks = store.list('Tasks').filter(function (t) { return t.job_id === job.id && (t.group === 'Booking' || t.group === 'Prebooking') && t.status !== 'Cancelled' && t.status !== 'Complete'; });
  return {
    sold_submission_id: job.sold_submission_id, booking_submission_id: job.booking_submission_id,
    booking_approved_at: job.booking_approved_at ? _s17Date(job.booking_approved_at) : null,
    booking_approved_by: job.booking_approved_by,
    sold_booking_match_status: job.sold_booking_match_status,
    outstanding_tasks: tasks.length,
    tasks: tasks.map(_s17TaskSummary)
  };
}

function _s17WorkSummary(store, job) {
  var packages = store.list('WorkPackages').filter(function (w) { return w.job_id === job.id; });
  var allocations = store.list('Allocations').filter(function (a) {
    return packages.some(function (w) { return w.id === a.work_package_id; }) && a.active === true;
  });
  var calls = store.list('Calls').filter(function (c) { return c.job_id === job.id; });
  var issues = store.list('Issues').filter(function (i) { return i.job_id === job.id && !['Resolved', 'Closed'].includes(i.status); });
  return {
    roof_required: job.roof_required, electrical_required: job.electrical_required, scaffold_required: job.scaffold_required,
    packages: packages.map(function (w) { return { id: w.id, trade: w.trade, status: w.status, planned_start: w.planned_start ? _s17Date(w.planned_start) : null, planned_end: w.planned_end ? _s17Date(w.planned_end) : null, commissioning_required: w.commissioning_required, actual_start: w.actual_start ? _s17Date(w.actual_start) : null, actual_end: w.actual_end ? _s17Date(w.actual_end) : null }; }),
    allocations: allocations.map(function (a) { return { id: a.id, work_package_id: a.work_package_id, person_id: a.person_id, role: a.role, start_at: a.start_at ? _s17Date(a.start_at) : null, end_at: a.end_at ? _s17Date(a.end_at) : null }; }),
    calls_count: calls.length,
    unresolved_issues: issues.length,
    operational_complete_at: job.operational_complete_at ? _s17Date(job.operational_complete_at) : null,
    customer_happy_at: job.customer_happy_at ? _s17Date(job.customer_happy_at) : null
  };
}

function _s17MaterialSummary(store, job) {
  var materials = store.list('Materials').filter(function (m) { return m.job_id === job.id; });
  var reservations = store.list('Reservations').filter(function (r) { return materials.some(function (m) { return m.id === r.material_id; }); });
  var orders = store.list('Orders').filter(function (o) { return o.job_id === job.id; });
  return {
    materials_count: materials.length,
    required_quantity: materials.reduce(function (s, m) { return s + (m.required_quantity || 0); }, 0),
    cancelled_quantity: materials.reduce(function (s, m) { return s + (m.cancelled_quantity || 0); }, 0),
    active_reservations: reservations.filter(function (r) { return r.status === 'Active'; }).length,
    orders: orders.map(function (o) { return { id: o.id, status: o.status, merchant_id: o.merchant_id, supplier_reference: o.supplier_reference }; })
  };
}

function _s17ScaffoldSummary(store, job) {
  var bookings = store.list('ScaffoldBookings').filter(function (b) { return b.job_id === job.id; });
  return {
    scaffold_required: job.scaffold_required,
    bookings: bookings.map(function (b) { return { id: b.id, status: b.status, erect_planned_at: b.erect_planned_at ? _s17Date(b.erect_planned_at) : null, erect_actual_at: b.erect_actual_at ? _s17Date(b.erect_actual_at) : null, strip_actual_at: b.strip_actual_at ? _s17Date(b.strip_actual_at) : null, company_id: b.company_id }; })
  };
}

function _s17CommissioningSummary(store, job) {
  var submissions = store.list('CommissioningSubmissions').filter(function (s) { return s.job_id === job.id; });
  var equipment = store.list('JobEquipment').filter(function (e) { return e.job_id === job.id; });
  return {
    submissions: submissions.map(function (s) { return { id: s.id, status: s.status, submitted_at: s.submitted_at ? _s17Date(s.submitted_at) : null, reviewed_at: s.reviewed_at ? _s17Date(s.reviewed_at) : null, installer_id: s.installer_id, work_package_id: s.work_package_id }; }),
    equipment_count: equipment.length,
    needs_review: submissions.some(function (s) { return s.status === 'Submitted'; })
  };
}

function _s17HandoverSummary(store, job) {
  var handover = store.list('Handover').filter(function (h) { return h.job_id === job.id; });
  return {
    status: job.handover_status,
    records: handover.map(function (h) { return { id: h.id, sent_at: h.sent_at ? _s17Date(h.sent_at) : null, status: h.status }; })
  };
}

function _s17FinanceSummary(store, job) {
  var stages = store.list('InvoiceStages').filter(function (s) { return s.job_id === job.id; });
  var payments = store.list('Payments');
  var stageDetails = stages.map(function (s) {
    var sp = payments.filter(function (p) { return p.invoice_stage_id === s.id; });
    var paid = sp.reduce(function (sum, p) { return sum + (p.amount_pence || 0); }, 0);
    return { stage_id: s.id, stage: s.stage, gross_pence: s.gross_pence || 0, paid_pence: paid, outstanding_pence: (s.gross_pence || 0) - paid, due_date: s.due_date ? _s17Date(s.due_date) : null, status: s.status, xero_invoice_id: s.xero_invoice_id || null };
  });
  return {
    original_gross_pence: job.original_gross_pence || job.current_contract_gross_pence || 0,
    deposit_confirmed: !!job.deposit_bank_confirmed_at,
    deposit_confirmed_at: job.deposit_bank_confirmed_at ? _s17Date(job.deposit_bank_confirmed_at) : null,
    deposit_confirmed_by: job.deposit_bank_confirmed_by,
    finance_route: job.finance_route,
    contract_status: job.contract_status,
    stages: stageDetails,
    total_invoiced: stageDetails.reduce(function (s, d) { return s + d.gross_pence; }, 0),
    total_paid: stageDetails.reduce(function (s, d) { return s + d.paid_pence; }, 0),
    total_outstanding: stageDetails.reduce(function (s, d) { return s + d.outstanding_pence; }, 0)
  };
}

function _s17CRMSummary(store, job) {
  var ghlTasks = store.list('GHLTasks').filter(function (g) { return g.job_id === job.id; });
  return {
    ghl_tasks: ghlTasks.map(function (g) { return { id: g.id, task_id: g.task_id, opportunity_id: g.opportunity_id, target_pipeline_id: g.target_pipeline_id, completed_at: g.completed_at ? _s17Date(g.completed_at) : null }; })
  };
}

function _s17CancellationSummary(store, job) {
  var cancelTasks = store.list('Tasks').filter(function (t) { return t.job_id === job.id && t.group === 'Cancellation' && !['Complete', 'NotRequired'].includes(t.status); });
  return {
    is_cancelled: ['CancellationInProgress', 'Cancelled'].includes(job.workflow_stage),
    cancellation_at: job.cancellation_at ? _s17Date(job.cancellation_at) : null,
    cancellation_by: job.cancellation_by,
    cancellation_reason: job.cancellation_reason,
    open_review_tasks: cancelTasks.length,
    tasks: cancelTasks.map(_s17TaskSummary)
  };
}

function _s17SystemSummary(store, job) {
  var pendingOutbox = store.list('Outbox').filter(function (o) {
    return !['Succeeded', 'Cancelled'].includes(o.status) && (o.correlation_id === job.id || o.correlation_id === 'XI-' + job.id + '-deposit' || o.correlation_id === 'XI-' + job.id + '-interim' || o.correlation_id === 'XI-' + job.id + '-final');
  });
  var auditCount = store.list('AuditEvents').filter(function (a) { return a.entity_id === job.id; }).length;
  return {
    pending_outbox: pendingOutbox.length,
    audit_events: auditCount
  };
}

/* --- 3. JOB SEARCH --- */

function _s17JobSearch(store, query) {
  _s17Scope(store);
  if (!_s17Text(query)) return [];
  var q = query.toLowerCase().trim();
  var jobs = store.list('Jobs');
  var customers = store.list('Customers');
  var results = [];

  for (var i = 0; i < jobs.length; i++) {
    var j = jobs[i];
    var cust = customers.filter(function (c) { return c.id === j.customer_id; })[0] || {};
    var match =
      (j.job_id && j.job_id.toLowerCase().indexOf(q) !== -1) ||
      (j.display_name && j.display_name.toLowerCase().indexOf(q) !== -1) ||
      (cust.last_name && cust.last_name.toLowerCase().indexOf(q) !== -1) ||
      (cust.postcode && cust.postcode.toLowerCase().indexOf(q) !== -1) ||
      (cust.email && cust.email.toLowerCase().indexOf(q) !== -1) ||
      (cust.phone && String(cust.phone).toLowerCase().indexOf(q) !== -1);
    if (match) {
      results.push({
        id: j.id, job_id: j.job_id, display_name: j.display_name,
        customer_name: [cust.first_name, cust.last_name].filter(Boolean).join(' ').trim(),
        postcode: cust.postcode || null,
        workflow_stage: j.workflow_stage, release_scope: j.release_scope
      });
    }
  }
  return results.slice(0, 50); // limit results
}

/* --- 4. OPERATIONAL QUEUES --- */

function _s17OperationalQueue(store, queueName) {
  _s17Scope(store);
  var allTasks = store.list('Tasks').filter(function (t) {
    return !['Complete', 'NotRequired', 'Cancelled'].includes(t.status);
  });

  var queueMap = {
    'booking': function () { return allTasks.filter(function (t) { return t.group === 'Booking' || t.group === 'Prebooking'; }); },
    'materials': function () { return allTasks.filter(function (t) { return t.group === 'Materials' || t.template_code === 'MAT01' || t.template_code === 'MAT05'; }); },
    'scaffold': function () { return allTasks.filter(function (t) { return t.group === 'Scaffold' || t.template_code === 'SCA01'; }); },
    'calls': function () { return allTasks.filter(function (t) { return t.group === 'Calls' || ['CAL01', 'INS01', 'INS04'].indexOf(t.template_code) >= 0; }); },
    'issues': function () { return allTasks.filter(function (t) { return t.related_entity_type === 'Issues'; }); },
    'commissioning': function () { return allTasks.filter(function (t) { return t.group === 'Commissioning'; }); },
    'handover': function () { return allTasks.filter(function (t) { return t.group === 'Handover'; }); },
    'payments': function () { return allTasks.filter(function (t) { return t.group === 'Finance' || t.template_code === 'FIN01' || t.template_code === 'FIN03'; }); },
    'ghl': function () { return allTasks.filter(function (t) { return t.group === 'CRM' || t.template_code === 'GHL01'; }); },
    'cancellation': function () { return allTasks.filter(function (t) { return t.group === 'Cancellation'; }); },
    'archive': function () { return allTasks.filter(function (t) { return t.group === 'System' && ['SYS01', 'SYS02'].includes(t.template_code); }); }
  };

  var filter = queueMap[queueName];
  if (!filter) return { queue: queueName, error: 'UNKNOWN_QUEUE', available: Object.keys(queueMap) };

  var tasks = filter().map(_s17TaskSummary);
  return { queue: queueName, count: tasks.length, tasks: tasks };
}

/* --- 5. ADMIN: RELEASE MODE STATUS --- */

function _s17AdminReleaseModes(store) {
  var modes = _s17Scope(store);
  var rows = store.list('ReleaseModes');
  return rows.map(function (r) {
    return {
      function_id: r.function_id,
      function_name: r.function_name,
      target_release: r.target_release,
      mode: r.mode,
      authorised_job_scope: r.authorised_job_scope,
      planned_target_mode: r.planned_target_mode,
      current_system: r.current_system,
      fallback: r.fallback,
      scope_boundary_notes: r.scope_boundary_notes,
      activation_time: r.activation_time,
      approved_version: r.approved_version,
      ben_approval_reference: r.ben_approval_reference
    };
  });
}

/* --- 6. ADMIN: SYSTEM STATUS --- */

function _s17AdminSystemStatus(store) {
  _s17Scope(store);
  var now = _s17Now();

  // Health checks
  var healthRows = store.list('HealthChecks');
  healthRows.sort(function (a, b) { return b.checked_at.localeCompare(a.checked_at); });
  var latestHealth = healthRows.length > 0 ? healthRows[0] : null;

  // CommitJournal
  var stalledCommits = store.list('CommitJournal').filter(function (j) { return j.state !== 'Committed'; });
  var recoveryNeeded = stalledCommits.filter(function (j) { return j.state === 'RecoveryRequired'; });

  // Outbox
  var uncertainOutbox = store.list('Outbox').filter(function (o) { return ['NeedsReview', 'RetryDue'].includes(o.status); });

  // NOT_CONFIGURED dependencies (derived from canonical known gaps)
  var notConfigured = [];
  var settings = store.list('Settings');
  function settingPresent(key) { return settings.some(function (s) { return s.key === key && s.typed_value !== 'NOT_CONFIGURED'; }); }

  // Check GHL configuration
  var ghlTasks = store.list('GHLTasks');
  var ghlConfigured = ghlTasks.some(function (g) { return g.opportunity_id && g.opportunity_id !== 'NOT_CONFIGURED'; });
  if (!ghlConfigured) notConfigured.push({ area: 'GHL pipeline/stage IDs', status: 'NOT_CONFIGURED', detail: 'No GHL opportunity_id configured' });

  // Check Xero configuration
  var xeroConfigured = store.list('InvoiceStages').some(function (s) { return s.xero_invoice_id && s.xero_invoice_id !== 'NOT_CONFIGURED'; });
  if (!xeroConfigured) notConfigured.push({ area: 'Xero API integration', status: 'NOT_CONFIGURED', detail: 'No real Xero invoice IDs present' });

  // Check backup destination
  var backupConfigured = store.list('ReportSnapshots').some(function (s) { return s.report_type === 'BackupManifest' && s.file_id; });
  if (!backupConfigured) notConfigured.push({ area: 'Backup Drive destination', status: 'NOT_CONFIGURED', detail: 'No Drive file_id on backup manifests' });

  // Check destructive restore
  notConfigured.push({ area: 'Destructive restore procedure', status: 'NOT_CONFIGURED', detail: 'Restore is dry-run only; full procedure not implemented' });

  // Check commissioning forms
  var commForms = store.list('CommissioningTemplates');
  if (commForms.length === 0) notConfigured.push({ area: 'Commissioning templates/forms', status: 'NOT_CONFIGURED', detail: 'No commissioning templates present' });

  // Check scaffolder contacts
  var scaffContacts = store.list('Contacts').filter(function (c) { return c.company_id && store.list('Companies').some(function (co) { return co.id === c.company_id && co.type === 'Scaffolder'; }); });
  if (scaffContacts.length === 0) notConfigured.push({ area: 'Scaffolder contacts', status: 'NOT_CONFIGURED', detail: 'No scaffolder company contacts configured' });

  return {
    generated_at: now,
    health: {
      latest_check: latestHealth ? { checked_at: latestHealth.checked_at, outcome: latestHealth.outcome, last_success: latestHealth.last_success, error_code: latestHealth.error_code } : null,
      total_checks: healthRows.length
    },
    commit_journal: {
      stalled: stalledCommits.length,
      recovery_required: recoveryNeeded.length,
      recovery_ids: recoveryNeeded.map(function (r) { return r.id; })
    },
    outbox: {
      uncertain: uncertainOutbox.length,
      uncertain_ids: uncertainOutbox.map(function (o) { return o.id; })
    },
    not_configured: notConfigured,
    not_configured_count: notConfigured.length
  };
}

/* --- 7. AUDIT / HISTORY --- */

function _s17AuditHistory(store, jobId) {
  _s17Scope(store);
  var job = store.get('Jobs', jobId);
  if (!job) return { job_id: jobId, found: false };

  var events = [];

  // AuditEvents
  var auditEvents = store.list('AuditEvents').filter(function (a) { return a.entity_id === job.id || a.correlation_id === job.id; });
  for (var i = 0; i < auditEvents.length; i++) {
    var ae = auditEvents[i];
    var beforeSummary = null, afterSummary = null;
    try {
      if (ae.before_json) { var bj = JSON.parse(ae.before_json); beforeSummary = Object.keys(bj).slice(0, 5).join(','); }
      if (ae.after_json) { var aj = JSON.parse(ae.after_json); afterSummary = Object.keys(aj).slice(0, 5).join(','); }
    } catch (e) { /* ignore parse errors */ }
    events.push({
      type: 'audit', timestamp: ae.timestamp, action: ae.action,
      entity_type: ae.entity_type, entity_id: ae.entity_id,
      actor: ae.initiating_actor, reason: ae.reason,
      before_keys: beforeSummary, after_keys: afterSummary
    });
  }

  // TaskEvents for this job's tasks
  var jobTasks = store.list('Tasks').filter(function (t) { return t.job_id === job.id; });
  var taskIds = jobTasks.map(function (t) { return t.id; });
  var taskEvents = store.list('TaskEvents').filter(function (te) { return taskIds.includes(te.task_id); });
  for (var j = 0; j < taskEvents.length; j++) {
    var te = taskEvents[j];
    events.push({
      type: 'task_event', timestamp: te.timestamp || te.created_at, action: te.action || te.event_type,
      task_id: te.task_id, actor: te.actor || te.initiating_actor, note: te.note || te.completion_note
    });
  }

  // IssueEvents for this job's issues
  var jobIssues = store.list('Issues').filter(function (i) { return i.job_id === job.id; });
  var issueIds = jobIssues.map(function (i) { return i.id; });
  var issueEvents = store.list('IssueEvents').filter(function (ie) { return issueIds.includes(ie.issue_id); });
  for (var k = 0; k < issueEvents.length; k++) {
    var ie = issueEvents[k];
    events.push({
      type: 'issue_event', timestamp: ie.timestamp || ie.created_at, action: ie.action || ie.event_type,
      issue_id: ie.issue_id, actor: ie.actor || ie.initiating_actor, note: ie.note
    });
  }

  // Sort chronologically
  events.sort(function (a, b) { return (a.timestamp || '').localeCompare(b.timestamp || ''); });

  return {
    job_id: jobId,
    found: true,
    total_events: events.length,
    audit_events: auditEvents.length,
    task_events: taskEvents.length,
    issue_events: issueEvents.length,
    events: events.slice(0, 200) // limit for display
  };
}

/* --- 8. ACTION AVAILABILITY --- */

function _s17ActionAvailability(store, jobId) {
  var modes = _s17Scope(store);
  var job = store.get('Jobs', jobId);
  if (!job) return { job_id: jobId, found: false };

  function fnMode(fnId) {
    var m = modes[fnId];
    if (!m) return 'Disabled';
    return m.mode;
  }
  function fnScope(fnId) {
    var m = modes[fnId];
    if (!m) return 'None';
    return m.authorised_job_scope;
  }
  function fnEnabled(fnId) {
    return fnMode(fnId) !== 'Disabled' && fnScope(fnId) !== 'None';
  }
  function pilotGated(fnId) {
    if (!fnEnabled(fnId)) return false;
    if (fnScope(fnId) === 'Pilot' && !job.pilot_job) return false;
    return true;
  }
  function combinedMode(fnIds) {
    var modes_ = fnIds.map(fnMode);
    if (modes_.some(function (m) { return m === 'Disabled'; })) return 'Disabled';
    if (modes_.some(function (m) { return m === 'Manual'; })) return 'Manual';
    return 'Automated';
  }

  var stage = job.workflow_stage;
  var operational = !!job.operational_complete_at;
  var cancelled = ['CancellationInProgress', 'Cancelled'].includes(stage);
  var archived = !!job.archived_at;

  return {
    job_id: jobId,
    found: true,
    workflow_stage: stage,
    actions: {
      record_call: { available: !cancelled && !archived && pilotGated('FN-01'), mode: fnEnabled('FN-01') ? fnMode('FN-01') : 'Disabled' },
      resolve_issue: { available: !cancelled && !archived && pilotGated('FN-01'), mode: fnEnabled('FN-01') ? fnMode('FN-01') : 'Disabled' },
      approve_booking: { available: !cancelled && !archived && ['Prebooking', 'ReadyToBook', 'BookingInProgress'].includes(stage) && pilotGated('FN-01'), mode: fnEnabled('FN-01') ? fnMode('FN-01') : 'Disabled' },
      operational_completion: { available: !cancelled && !archived && ['InProgress', 'Aftercare'].includes(stage) && !operational && pilotGated('FN-01'), mode: fnEnabled('FN-01') ? fnMode('FN-01') : 'Disabled' },
      commissioning_review: { available: !cancelled && !archived && pilotGated('FN-07'), mode: fnEnabled('FN-07') ? fnMode('FN-07') : 'Disabled' },
      handover_approval: { available: !cancelled && !archived && pilotGated('FN-08'), mode: fnEnabled('FN-08') ? fnMode('FN-08') : 'Disabled' },
      deposit_confirmation: { available: !cancelled && !archived && !job.deposit_bank_confirmed_at && pilotGated('FN-15'), mode: fnEnabled('FN-15') ? fnMode('FN-15') : 'Disabled' },
      cancel_job: { available: !cancelled && !archived && fnEnabled('FN-01') && fnEnabled('FN-17'), mode: combinedMode(['FN-01', 'FN-17']), note: 'FN-01 + FN-17 required; GHL cancellation task is Manual' },
      reinstate_job: { available: stage === 'Cancelled' && fnEnabled('FN-01') && fnEnabled('FN-17'), mode: combinedMode(['FN-01', 'FN-17']) },
      archive_job: { available: !archived && operational && pilotGated('FN-13'), mode: fnEnabled('FN-13') ? fnMode('FN-13') : 'Disabled' },
      ghl_progression: { available: !cancelled && !archived && pilotGated('FN-11'), mode: fnEnabled('FN-11') ? fnMode('FN-11') : 'Disabled' }
    }
  };
}

/* --- 9. TASK ACTION AVAILABILITY --- */

function _s17TaskActionAvailability(store, taskId) {
  _s17Scope(store);
  var task = store.get('Tasks', taskId);
  if (!task) return { task_id: taskId, found: false };

  var completable = ['Open', 'Waiting', 'InProgress'].includes(task.status) && !task.revision_required;
  var blockable = task.status === 'Blocked';
  var alreadyDone = ['Complete', 'NotRequired', 'Cancelled'].includes(task.status);

  return {
    task_id: taskId,
    found: true,
    job_id: task.job_id,
    status: task.status,
    title: task.title,
    actions: {
      complete: { available: completable, note: completable ? null : (alreadyDone ? 'Already ' + task.status : 'Status ' + task.status + ' not completable') },
      reopen: { available: task.status === 'Complete', note: task.status === 'Complete' ? null : 'Not complete' }
    }
  };
}

if (typeof module !== 'undefined') {
  module.exports = {
    S17_ADMIN_DEV_SHEET_ID,
    _s17Date, _s17Today, _s17GuardStore, _s17Scope,
    _s17OfficeToday, _s17JobOverview, _s17JobSearch,
    _s17OperationalQueue, _s17AdminReleaseModes, _s17AdminSystemStatus,
    _s17AuditHistory, _s17ActionAvailability, _s17TaskActionAvailability
  };
}

/* Minimal synthetic S17 fixture. DEV synthetic only. Reuses S16 fixture Jobs. */
'use strict';

function _s17FixtureRows() {
  var n = '2025-02-01T12:00:00.000Z';
  var base = { created_at: n, created_by: 'S17', updated_at: n, updated_by: 'S17', version: 1, source_system: 'S17-fixture', commit_id: 'S17-fixture' };

  // Reuse S16 job IDs — verify compatibility
  var oldJob = Object.assign({}, base, {
    id: 'J-s16-old', job_id: 'SS-S16O-LD01', customer_id: 'CUST-s17',
    display_name: 'S17 Old Completed Job',
    sold_submission_id: null, booking_submission_id: null,
    sold_at: '2025-01-15T00:00:00.000Z', salesperson_id: null, lead_source: 'S17',
    quote_reference: 'Q-S17-OLD', presale_file_id: null,
    finance_route: 'Standard', contract_status: 'Signed', contract_id: 'C-S17-OLD',
    contract_signed_at: '2025-01-20T00:00:00.000Z', contract_evidence_id: null,
    original_net_pence: 400000, original_vat_pence: 80000, original_gross_pence: 480000,
    approved_change_pence: null, current_contract_gross_pence: 480000,
    valuation_basis: 'Standard', sold_booking_match_status: 'Match',
    customer_details_verified_at: '2025-01-20T00:00:00.000Z', customer_details_verified_by: 'PERSON-s17-office',
    deposit_bank_confirmed_at: '2025-02-01T00:00:00.000Z', deposit_bank_confirmed_by: 'PERSON-s17-ben',
    deposit_bank_reference: 'DEP-S17-OLD',
    roof_required: true, electrical_required: false, scaffold_required: false,
    workflow_stage: 'OperationallyComplete',
    booking_approved_at: '2025-01-25T00:00:00.000Z', booking_approved_by: 'PERSON-s17-office',
    operational_complete_at: '2025-02-01T00:00:00.000Z', operational_complete_by: 'PERSON-s17-office',
    customer_happy_at: null, customer_happy_by: null,
    handover_status: 'Sent', financial_status: 'Complete',
    cancellation_at: null, cancellation_by: null, cancellation_reason: null,
    archived_at: null, next_action_at: null, account_policy_version: null,
    pilot_job: true, release_scope: 'R1', source_record_id: null
  });

  var activeJob = Object.assign({}, base, {
    id: 'J-s17-active', job_id: 'SS-S17A-CTIVE', customer_id: 'CUST-s17',
    display_name: 'S17 Active Job', sold_submission_id: null, booking_submission_id: null,
    sold_at: '2026-07-01T00:00:00.000Z', salesperson_id: null, lead_source: 'S17',
    quote_reference: 'Q-S17-ACTIVE', presale_file_id: null,
    finance_route: 'Standard', contract_status: 'Signed', contract_id: 'C-S17-ACTIVE',
    contract_signed_at: '2026-07-15T00:00:00.000Z', contract_evidence_id: null,
    original_net_pence: 500000, original_vat_pence: 100000, original_gross_pence: 600000,
    approved_change_pence: null, current_contract_gross_pence: 600000,
    valuation_basis: 'Standard', sold_booking_match_status: 'Match',
    customer_details_verified_at: '2026-07-15T00:00:00.000Z', customer_details_verified_by: 'PERSON-s17-office',
    deposit_bank_confirmed_at: '2026-08-01T00:00:00.000Z', deposit_bank_confirmed_by: 'PERSON-s17-ben',
    deposit_bank_reference: 'DEP-S17-ACTIVE',
    roof_required: true, electrical_required: true, scaffold_required: true,
    workflow_stage: 'Booked',
    booking_approved_at: '2026-07-20T00:00:00.000Z', booking_approved_by: 'PERSON-s17-office',
    operational_complete_at: null, operational_complete_by: null,
    customer_happy_at: null, customer_happy_by: null,
    handover_status: 'NotReady', financial_status: 'Pending',
    cancellation_at: null, cancellation_by: null, cancellation_reason: null,
    archived_at: null, next_action_at: '2026-09-15T00:00:00.000Z',
    account_policy_version: null, pilot_job: true, release_scope: 'R1', source_record_id: null
  });

  var openTasks = [
    Object.assign({}, base, {
      id: 'TASK-s17-overdue', job_id: 'J-s17-active', template_code: 'BKG04',
      instance_key: 'S17-overdue', group: 'Booking', title: 'Overdue booking task',
      owner_id: 'PERSON-s17-office', related_entity_type: 'Jobs', related_entity_id: 'J-s17-active',
      due_at: '2026-08-01T00:00:00.000Z', original_due_at: '2026-08-01T00:00:00.000Z',
      status: 'Open', priority: 1, blocking_reason: null,
      created_rule_version: 'S17-1.0', source_system: 'S17-fixture'
    }),
    Object.assign({}, base, {
      id: 'TASK-s17-today', job_id: 'J-s17-active', template_code: 'MAT01',
      instance_key: 'S17-today', group: 'Materials', title: 'Order materials',
      owner_id: 'PERSON-s17-office', related_entity_type: 'Jobs', related_entity_id: 'J-s17-active',
      due_at: '2026-09-06T00:00:00.000Z', original_due_at: '2026-09-06T00:00:00.000Z',
      status: 'Open', priority: 1, blocking_reason: null,
      created_rule_version: 'S17-1.0', source_system: 'S17-fixture'
    }),
    Object.assign({}, base, {
      id: 'TASK-s17-soon', job_id: 'J-s17-active', template_code: 'SCA01',
      instance_key: 'S17-soon', group: 'Scaffold', title: 'Book scaffold',
      owner_id: 'PERSON-s17-office', related_entity_type: 'Jobs', related_entity_id: 'J-s17-active',
      due_at: '2026-09-10T00:00:00.000Z', original_due_at: '2026-09-10T00:00:00.000Z',
      status: 'Open', priority: 1, blocking_reason: null,
      created_rule_version: 'S17-1.0', source_system: 'S17-fixture'
    }),
    Object.assign({}, base, {
      id: 'TASK-s17-complete', job_id: 'J-s17-active', template_code: 'BKG04',
      instance_key: 'S17-complete', group: 'Booking', title: 'Completed task',
      owner_id: 'PERSON-s17-office', related_entity_type: 'Jobs', related_entity_id: 'J-s17-active',
      due_at: '2026-08-15T00:00:00.000Z', original_due_at: '2026-08-15T00:00:00.000Z',
      status: 'Complete', completed_at: '2026-08-15T00:00:00.000Z', completed_by: 'PERSON-s17-office',
      completion_note: 'Done', created_rule_version: 'S17-1.0', source_system: 'S17-fixture'
    })
  ];

  var people = [
    Object.assign({}, base, { id: 'PERSON-s17-office', email: 's17-office@dev.example.invalid', display_name: 'Tanya S17', role: 'Office', active: true }),
    Object.assign({}, base, { id: 'PERSON-s17-ben', email: 's17-ben@dev.example.invalid', display_name: 'Ben S17', role: 'Manager', active: true })
  ];

  var workPackages = [
    Object.assign({}, base, { id: 'WP-s17-roof', job_id: 'J-s17-active', trade: 'Roof', required: true, status: 'Scheduled', planned_start: '2026-09-15', planned_end: '2026-09-16', revision: 1, sequence: 1, commissioning_required: true }),
    Object.assign({}, base, { id: 'WP-s17-elec', job_id: 'J-s17-active', trade: 'Electrical', required: true, status: 'Unscheduled', planned_start: null, planned_end: null, revision: 1, sequence: 2, commissioning_required: false })
  ];

  var allocations = [
    Object.assign({}, base, { id: 'ALLOC-s17', work_package_id: 'WP-s17-roof', person_id: 'PERSON-s17-office', role: 'Lead', active: true, start_at: '2026-09-15', end_at: '2026-09-16' })
  ];

  var invoiceStages = [
    Object.assign({}, base, { id: 'INV-s17-deposit', job_id: 'J-s17-active', stage: 'deposit', gross_pence: 150000, status: 'Confirmed', due_date: '2026-08-01', source_status: 'PAID', xero_invoice_id: null }),
    Object.assign({}, base, { id: 'INV-s17-interim', job_id: 'J-s17-active', stage: 'interim', gross_pence: 210000, status: 'Sent', due_date: '2026-09-11', source_status: 'AUTHORISED', xero_invoice_id: null })
  ];

  var scaffoldBookings = [
    Object.assign({}, base, { id: 'SCA-s17', job_id: 'J-s17-active', status: 'Confirmed', company_id: null, erect_planned_at: '2026-09-14', erect_actual_at: null, strip_actual_at: null, revision: 1 })
  ];

  var auditEvents = [
    Object.assign({}, base, {
      id: 'AE-s17-1', entity_type: 'Jobs', entity_id: 'J-s17-active',
      action: 'S04CompleteTask', before_json: '{"status":"Open"}', after_json: '{"status":"Complete"}',
      initiating_actor: 'PERSON-s17-office', executing_service: 'S04 DEV',
      timestamp: '2026-08-15T10:00:00.000Z', correlation_id: 'S17-CMD-1', reason: 'Synthetic test event',
      commit_id: 'S17-fixture', source_system: undefined
    }),
    Object.assign({}, base, {
      id: 'AE-s17-2', entity_type: 'Jobs', entity_id: 'J-s17-active',
      action: 'S05Booking', before_json: null, after_json: '{"booking_submission_id":"S17-booking"}',
      initiating_actor: 'PERSON-s17-office', executing_service: 'S05 DEV',
      timestamp: '2026-07-20T09:00:00.000Z', correlation_id: 'S17-CMD-2', reason: 'Booking intake',
      commit_id: 'S17-fixture', source_system: undefined
    })
  ];

  // Shared canonical SYS01/SYS02 (compatible with S02 seed)
  var sysTemplates = [
    Object.assign({}, base, { source_system: undefined, id: 'TPL-SYS01', template_code: 'SYS01', title: 'Check authorisation/integration health', group: 'System', default_owner_role: 'Office', trigger_event: 'Every staffed day', due_rule: '09:00', evidence_required: 'Last-success timestamps, failures assigned', active: true, template_version: 'S17-1.0' }),
    Object.assign({}, base, { source_system: undefined, id: 'TPL-SYS02', template_code: 'SYS02', title: 'End-of-day review', group: 'System', default_owner_role: 'Office', trigger_event: 'Every staffed day', due_rule: '16:30', evidence_required: 'Unresolved actions assigned', active: true, template_version: 'S17-1.0' })
  ];

  // Shared prior-stage prerequisites (must NOT require created_by === 'S17')
  // J-s16-old: S16 fixture job, S17 reads it for archive eligibility checks
  var sharedJobs = [oldJob];

  return {
    shared: { Jobs: sharedJobs, TaskTemplates: sysTemplates },
    owned: {
      Jobs: [activeJob],
      Tasks: openTasks,
      People: people,
      WorkPackages: workPackages,
      Allocations: allocations,
      InvoiceStages: invoiceStages,
      ScaffoldBookings: scaffoldBookings,
      AuditEvents: auditEvents
    }
  };
}

/* Verify a shared prior-stage Job is compatible with S17 read-model requirements. */
function _s17ValidateSharedJob(existing, required) {
  var fields = ['workflow_stage', 'handover_status', 'pilot_job', 'release_scope'];
  var mismatches = [];
  for (var i = 0; i < fields.length; i++) {
    var f = fields[i];
    if (String(existing[f]) !== String(required[f])) mismatches.push(f + ': expected=' + required[f] + ' actual=' + existing[f]);
  }
  // operational_complete_at must be present for archive eligibility
  if (!existing.operational_complete_at && required.operational_complete_at) mismatches.push('operational_complete_at: expected set, actual missing');
  // archived_at must be null (S17 needs it unarchived for archive smoke)
  if (existing.archived_at && required.archived_at === null) mismatches.push('archived_at: expected null, actual ' + existing.archived_at);
  return { compatible: mismatches.length === 0, mismatches: mismatches };
}

function _s17ValidateSharedTemplate(existing, required) {
  var fields = ['template_code', 'group', 'default_owner_role', 'trigger_event', 'due_rule'];
  var mismatches = [];
  for (var i = 0; i < fields.length; i++) {
    var f = fields[i];
    if (existing[f] !== required[f]) mismatches.push(f + ': expected=' + required[f] + ' actual=' + existing[f]);
  }
  if (existing.active !== true) mismatches.push('active: expected=true actual=' + existing.active);
  return { compatible: mismatches.length === 0, mismatches: mismatches };
}

function _s17Seed(store) {
  var data = _s17FixtureRows();
  var shared = data.shared;
  var owned = data.owned;

  // Phase 1a: Shared TaskTemplates — reuse if compatible, insert if absent
  if (shared.TaskTemplates) {
    for (var i = 0; i < shared.TaskTemplates.length; i++) {
      var trow = shared.TaskTemplates[i];
      delete trow.source_system;
      var texisting = store.get('TaskTemplates', trow.id);
      if (texisting) {
        var tcompat = _s17ValidateSharedTemplate(texisting, trow);
        if (!tcompat.compatible) throw new Error('S17_REFUSED: incompatible shared template ' + trow.id + ': ' + tcompat.mismatches.join('; '));
      } else {
        store.insert('TaskTemplates', trow);
      }
    }
  }

  // Phase 1b: Shared prior-stage fixture Jobs — fail if absent, verify if present
  if (shared.Jobs) {
    for (var j = 0; j < shared.Jobs.length; j++) {
      var jrow = shared.Jobs[j];
      var jexisting = store.get('Jobs', jrow.id);
      if (!jexisting) throw new Error('S17_REFUSED: shared prerequisite ' + jrow.id + ' not found — run prior-stage fixture first');
      var jcompat = _s17ValidateSharedJob(jexisting, jrow);
      if (!jcompat.compatible) throw new Error('S17_REFUSED: incompatible shared Job ' + jrow.id + ': ' + jcompat.mismatches.join('; '));
      // Compatible — reuse, do not mutate
    }
  }

  // Phase 2: S17-owned synthetic rows — strict collision protection
  for (var table in owned) {
    if (!owned.hasOwnProperty(table)) continue;
    for (var i = 0; i < owned[table].length; i++) {
      var row2 = owned[table][i];
      // Strip columns not in AuditEvents schema (no created_by/updated_by/version/source_system)
      if (table === 'AuditEvents') {
        delete row2.source_system;
        delete row2.created_by;
        delete row2.updated_by;
        delete row2.updated_at;
        delete row2.version;
      }
      var existing2 = store.get(table, row2.id);
      // AuditEvents has no created_by — skip provenance check
      if (table !== 'AuditEvents' && existing2 && existing2.created_by !== 'S17') throw new Error('S17_REFUSED: fixture ID collision ' + row2.id);
      if (!existing2) store.insert(table, row2);
    }
  }
}

function _s17VerifyRow(store, table, id, expectedCreatedBy) {
  var rows = store.list(table);
  var matching = rows.filter(function (r) { return r.id === id; });
  if (matching.length === 0) {
    var ids = rows.map(function (r) { return r.id; });
    return { ok: false, table: table, expected_id: id, total_rows: rows.length, found_ids: ids, detail: 'Row not found' };
  }
  if (matching.length > 1) return { ok: false, table: table, expected_id: id, count: matching.length, detail: 'Duplicate rows' };
  var row = matching[0];
  if (expectedCreatedBy && row.created_by !== expectedCreatedBy)
    return { ok: false, table: table, id: id, expected_created_by: expectedCreatedBy, actual_created_by: row.created_by, detail: 'created_by mismatch' };
  return { ok: true, table: table, id: id, created_by: row.created_by };
}

function _s17Smoke(store, core) {
  function fail(step, detail) { return { pass: false, step: step, detail: detail }; }

  // 1. Office today
  var today;
  try { today = core._s17OfficeToday(store, { as_of: '2026-09-06' }); } catch (e) { return fail('office-today', e.message || String(e)); }
  if (typeof today.overdue_count !== 'number') return fail('office-today', 'missing overdue_count');

  // 2. Job overview
  var overview;
  try { overview = core._s17JobOverview(store, 'J-s17-active'); } catch (e) { return fail('job-overview', e.message || String(e)); }
  if (!overview.found) return fail('job-overview', 'job not found');
  if (!overview.identity) return fail('job-overview', 'missing identity');
  if (!overview.booking) return fail('job-overview', 'missing booking');
  if (!overview.work) return fail('job-overview', 'missing work');
  if (!overview.finance) return fail('job-overview', 'missing finance');

  // 3. Job search
  var search;
  try { search = core._s17JobSearch(store, 'S17'); } catch (e) { return fail('job-search', e.message || String(e)); }
  if (!Array.isArray(search)) return fail('job-search', 'not an array');

  // 4. Operational queue
  var queue;
  try { queue = core._s17OperationalQueue(store, 'booking'); } catch (e) { return fail('operational-queue', e.message || String(e)); }
  if (typeof queue.count !== 'number') return fail('operational-queue', 'missing count');

  // 5. Admin ReleaseModes
  var modes;
  try { modes = core._s17AdminReleaseModes(store); } catch (e) { return fail('admin-modes', e.message || String(e)); }
  if (!Array.isArray(modes) || modes.length === 0) return fail('admin-modes', 'no modes returned');

  // 6. Admin system status
  var status;
  try { status = core._s17AdminSystemStatus(store); } catch (e) { return fail('admin-status', e.message || String(e)); }
  if (!status.health) return fail('admin-status', 'missing health');
  if (!status.not_configured) return fail('admin-status', 'missing not_configured');

  // 7. Audit history
  var history;
  try { history = core._s17AuditHistory(store, 'J-s17-active'); } catch (e) { return fail('audit-history', e.message || String(e)); }
  if (!history.found) return fail('audit-history', 'job not found');
  if (typeof history.total_events !== 'number') return fail('audit-history', 'missing total_events');

  // 8. Action availability
  var actions;
  try { actions = core._s17ActionAvailability(store, 'J-s17-active'); } catch (e) { return fail('action-availability', e.message || String(e)); }
  if (!actions.found) return fail('action-availability', 'job not found');
  if (!actions.actions) return fail('action-availability', 'missing actions');
  if (actions.actions.complete_task) return fail('action-availability', 'complete_task must not be a per-job action');

  // 9. Task action availability
  var taskActions;
  try { taskActions = core._s17TaskActionAvailability(store, 'TASK-s17-today'); } catch (e) { return fail('task-actions', e.message || String(e)); }
  if (!taskActions.found) return fail('task-actions', 'task not found');
  if (!taskActions.actions.complete) return fail('task-actions', 'missing complete action');

  return {
    pass: true,
    detail: {
      overdue_count: today.overdue_count,
      due_today_count: today.due_today_count,
      job_stage: overview.identity.workflow_stage,
      search_results: search.length,
      queue_count: queue.count,
      mode_count: modes.length,
      not_configured_count: status.not_configured_count,
      audit_events: history.total_events,
      actions_count: Object.keys(actions.actions).length,
      task_completable: taskActions.actions.complete.available,
      external_calls: 0
    }
  };
}

if (typeof module !== 'undefined') module.exports = { _s17FixtureRows, _s17Seed, _s17VerifyRow, _s17Smoke };

/* Header-checked enumeration adapter. S17 is read-only — no ScriptLock required for mutations. */
function _s17CloudGuard() {
  var ss = SpreadsheetApp.openById(S17_ADMIN_DEV_SHEET_ID);
  if (ss.getId() !== S17_ADMIN_DEV_SHEET_ID) throw new Error('S17_REFUSED: exact DEV sheet required');
  return ss;
}
function _s17Sheet(ss, name) {
  var matches = ss.getSheets().filter(function (s) { return s.getName() === name; });
  if (matches.length !== 1) throw new Error('S17_SCHEMA: missing/duplicate tab ' + name);
  var sh = matches[0], headers = S17_HEADERS[name];
  if (!headers || sh.getLastColumn() !== headers.length || JSON.stringify(sh.getRange(1, 1, 1, headers.length).getValues()[0]) !== JSON.stringify(headers))
    throw new Error('S17_SCHEMA: header mismatch ' + name);
  return sh;
}
function _s17Cell(value) { return value === null || value === undefined ? '' : typeof value === 'string' && /^[=+@'\-]/.test(value) ? "'" + value : value; }
function _s17CloudStore() {
  var ss = _s17CloudGuard();
  function list(name) {
    var sh = _s17Sheet(ss, name), h = S17_HEADERS[name];
    if (sh.getLastRow() < 2) return [];
    return sh.getRange(2, 1, sh.getLastRow() - 1, h.length).getValues().map(function (row) {
      var x = {}; h.forEach(function (k, i) { x[k] = row[i] === '' ? null : row[i]; }); return x;
    }).filter(function (r) { return r.id; });
  }
  return {
    getSheetId: function () { return ss.getId(); },
    getEnvironment: function () { _s17CloudGuard(); return 'DEV'; },
    list: list,
    get: function (name, id) {
      var rows = list(name).filter(function (r) { return r.id === id; });
      if (rows.length > 1) throw new Error('S17_SCHEMA: duplicate ID ' + id);
      return rows[0] || null;
    },
    insert: function (name, row) {
      var sh = _s17Sheet(ss, name), h = S17_HEADERS[name];
      if (list(name).some(function (r) { return r.id === row.id; })) throw new Error('S17_SCHEMA: duplicate insert ' + row.id);
      if (sh.getLastRow() >= sh.getMaxRows()) throw new Error('S17_CAPACITY: ' + name);
      for (var k in row) if (row.hasOwnProperty(k) && !h.includes(k)) throw new Error('S17_SCHEMA: unknown field ' + name + '.' + k);
      sh.getRange(sh.getLastRow() + 1, 1, 1, h.length).setValues([h.map(function (k) { return _s17Cell(row[k]); })]);
      SpreadsheetApp.flush();
    },
    update: function (name, id, patch) {
      var sh = _s17Sheet(ss, name), h = S17_HEADERS[name];
      var values = sh.getRange(2, 1, Math.max(1, sh.getLastRow() - 1), h.length).getValues();
      var indices = [];
      values.forEach(function (r, i) { if (r[0] === id) indices.push(i); });
      if (indices.length !== 1) throw new Error('S17_SCHEMA: update row ' + id);
      var idx = indices[0], row = values[idx];
      for (var k in patch) {
        if (!patch.hasOwnProperty(k)) continue;
        if (!h.includes(k)) throw new Error('S17_SCHEMA: unknown field ' + name + '.' + k);
        row[h.indexOf(k)] = patch[k];
      }
      sh.getRange(idx + 2, 1, 1, h.length).setValues([row.map(_s17Cell)]);
      SpreadsheetApp.flush();
    },
    withLock: function (fn) { try { return fn(); } finally {} }
  };
}
function _s17Result(name, fn) {
  try {
    var result = fn();
    var r = { test: name, pass: result.pass === undefined ? result.ok !== false : result.pass, detail: result.detail || result };
    console.log(JSON.stringify(r));
    return r;
  } catch (e) {
    var r = { test: name, pass: false, detail: String(e.message || e) };
    console.log(JSON.stringify(r));
    return r;
  }
}
function runS17FixtureDryRun() { return _s17Result('S17 dry run', function () { var s = _s17CloudStore(); for (var n in S17_HEADERS) { if (S17_HEADERS.hasOwnProperty(n)) s.list(n); } _s17Scope(s); return { ok: true, existing_job: !!s.get('Jobs', 'J-s17-active') }; }); }
function runS17FixtureApply() { return _s17Result('S17 fixture apply', function () { var s = _s17CloudStore(); return s.withLock(function () { _s17Seed(s); return { ok: true }; }); }); }
function runS17FixtureValidate() { return _s17Result('S17 fixture validate', function () { var s = _s17CloudStore(), data = _s17FixtureRows(); function verifyRows(rows, expectedCreatedBy) { for (var t in rows) { if (!rows.hasOwnProperty(t)) continue; for (var i = 0; i < rows[t].length; i++) { var r = rows[t][i]; var v = _s17VerifyRow(s, t, r.id, expectedCreatedBy); if (!v.ok) throw new Error('S17_FIXTURE: ' + t + '/' + r.id + ' — ' + v.detail); } } } verifyRows(data.shared, null); /* Owned tables with created_by */ var ownedWithCreatedBy = {}; for (var t in data.owned) { if (t !== 'AuditEvents') ownedWithCreatedBy[t] = data.owned[t]; } verifyRows(ownedWithCreatedBy, 'S17'); /* AuditEvents: no created_by column */ if (data.owned.AuditEvents) verifyRows({ AuditEvents: data.owned.AuditEvents }, null); return { ok: true }; }); }
function runS17HappyPathTest() { return _s17Result('S17 happy path', function () { return _s17Smoke(_s17CloudStore(), typeof S17_ADMIN_EXPORTS !== 'undefined' ? S17_ADMIN_EXPORTS : { _s17OfficeToday: _s17OfficeToday, _s17JobOverview: _s17JobOverview, _s17JobSearch: _s17JobSearch, _s17OperationalQueue: _s17OperationalQueue, _s17AdminReleaseModes: _s17AdminReleaseModes, _s17AdminSystemStatus: _s17AdminSystemStatus, _s17AuditHistory: _s17AuditHistory, _s17ActionAvailability: _s17ActionAvailability, _s17TaskActionAvailability: _s17TaskActionAvailability, _s17Scope: _s17Scope }); }); }
function runS17AppSheetSmokeSetup() { return _s17Result('S17 AppSheet smoke setup', function () { var s = _s17CloudStore(); return s.withLock(function () { var people = s.list('People').filter(function (p) { return p.email && p.email.trim().toLowerCase() === 'lenny@simplesolarltd.co.uk' && p.active === true; }); if (people.length !== 1) throw new Error('S17_SMOKE: expected exactly one active People record for lenny@simplesolarltd.co.uk, found ' + people.length); var lennyId = people[0].id; var now = new Date().toISOString(); var created = []; function ensureRole(role) { var existing = s.list('PersonRoles').filter(function (r) { return r.person_id === lennyId && r.role === role && r.active === true; }); if (!existing.length) { var rid = 'ROLE-S17-lenny-' + role.toLowerCase(); if (!s.get('PersonRoles', rid)) { s.insert('PersonRoles', { id: rid, person_id: lennyId, role: role, active: true, created_at: now, created_by: 'S17-smoke', updated_at: now, updated_by: 'S17-smoke', version: 1, source_system: 'S17-smoke', commit_id: 'S17-smoke' }); created.push('PersonRoles:' + rid); } } } function ensureTask(id, jobId, templateCode, title, group, version) { var t = s.get('Tasks', id); if (!t) { s.insert('Tasks', { id: id, job_id: jobId, template_code: templateCode, instance_key: 'S17-SMOKE-' + id, group: group, title: title, owner_id: lennyId, backup_id: null, related_entity_type: 'Jobs', related_entity_id: jobId, due_at: '2026-12-31', original_due_at: '2026-12-31', priority: 1, status: 'Open', blocking_reason: null, next_followup_at: null, completed_at: null, completed_by: null, completion_note: null, evidence_id: null, revision_required: false, created_rule_version: 'S17-smoke-1.0', created_at: now, created_by: 'S17-smoke', updated_at: now, updated_by: 'S17-smoke', version: version || 1, source_system: 'S17-smoke', commit_id: 'S17-smoke' }); created.push('Tasks:' + id); } else { created.push('Tasks:' + id + ' (exists v' + t.version + ')'); } } function ensureJob(id, jobId, stage) { var j = s.get('Jobs', id); if (!j) { s.insert('Jobs', { id: id, job_id: jobId, customer_id: 'CUST-s17', display_name: 'S17 AppSheet Smoke ' + stage, sold_submission_id: null, booking_submission_id: null, sold_at: null, salesperson_id: null, lead_source: 'S17-smoke', quote_reference: null, presale_file_id: null, finance_route: 'Standard', contract_status: 'Signed', contract_id: null, contract_signed_at: null, contract_evidence_id: null, original_net_pence: 1000, original_vat_pence: 200, original_gross_pence: 1200, approved_change_pence: null, current_contract_gross_pence: 1200, valuation_basis: 'Standard', sold_booking_match_status: 'Match', customer_details_verified_at: null, customer_details_verified_by: null, deposit_bank_confirmed_at: null, deposit_bank_confirmed_by: null, deposit_bank_reference: null, roof_required: false, electrical_required: false, scaffold_required: false, workflow_stage: stage, booking_approved_at: null, booking_approved_by: null, operational_complete_at: null, operational_complete_by: null, customer_happy_at: null, customer_happy_by: null, handover_status: 'NotReady', financial_status: 'Pending', cancellation_at: null, cancellation_by: null, cancellation_reason: null, archived_at: null, next_action_at: null, account_policy_version: null, pilot_job: true, release_scope: 'R1', created_at: now, created_by: 'S17-smoke', updated_at: now, updated_by: 'S17-smoke', version: 1, source_system: 'S17-smoke', source_record_id: null, commit_id: 'S17-smoke' }); created.push('Jobs:' + id); } else { created.push('Jobs:' + id + ' (exists stage=' + j.workflow_stage + ')'); } } function ensureIssue(id, jobId) { var iss = s.get('Issues', id); if (!iss) { s.insert('Issues', { id: id, job_id: jobId, work_package_id: null, type: 'Remedial', category: 'Smoke', description: 'AppSheet R1 ISSUE_UPDATE smoke test', raised_at: now, raised_by: lennyId, responsible_person_id: null, responsible_company_id: null, office_owner_id: lennyId, severity: 'Normal', status: 'Open', due_at: '2026-12-31', next_followup_at: null, blocks_completion: false, blocks_strip: false, estimated_value_pence: null, approved_value_pence: null, approval_status: 'NotRequired', approved_at: null, approved_by: null, resolution: null, resolved_at: null, closed_at: null, closed_by: null, customer_resolution_confirmed: null, linked_return_package_id: null, evidence_folder_id: null, created_at: now, created_by: 'S17-smoke', updated_at: now, updated_by: 'S17-smoke', version: 1, source_system: 'S17-smoke', commit_id: 'S17-smoke' }); created.push('Issues:' + id); } else { created.push('Issues:' + id + ' (exists v' + iss.version + ')'); } } ensureRole('Office'); ensureTask('TASK-s17-smoke-complete', 'J-s17-active', 'BKG04', 'AppSheet R1 TASK_COMPLETE smoke test', 'Booking'); ensureTask('TASK-s17-smoke-call', 'J-s17-active', 'INS01', 'AppSheet R1 CALL_RECORD smoke test', 'Calls'); ensureIssue('ISSUE-s17-smoke', 'J-s17-active'); ensureJob('J-s17-smoke-cancel', 'SS-S17S-MOKE-CANCEL', 'Booked'); var wp = s.get('WorkPackages', 'WP-s17-roof'); var plannerNote = wp ? 'WP-s17-roof exists v' + wp.version : 'WP-s17-roof MISSING'; return { ok: true, lenny_person_id: lennyId, created: created, planner_status: plannerNote, fixtures: { TASK_COMPLETE: { task_id: 'TASK-s17-smoke-complete', job_id: 'J-s17-active', owner_id: lennyId, version: (s.get('Tasks', 'TASK-s17-smoke-complete') || {}).version || 1 }, CALL_RECORD: { task_id: 'TASK-s17-smoke-call', job_id: 'J-s17-active', owner_id: lennyId, version: (s.get('Tasks', 'TASK-s17-smoke-call') || {}).version || 1 }, ISSUE_UPDATE: { issue_id: 'ISSUE-s17-smoke', job_id: 'J-s17-active', version: (s.get('Issues', 'ISSUE-s17-smoke') || {}).version || 1 }, PLANNER_UPDATE: { work_package_id: 'WP-s17-roof', job_id: 'J-s17-active', version: wp ? wp.version : null }, CANCEL_JOB: { job_id: 'J-s17-smoke-cancel', version: (s.get('Jobs', 'J-s17-smoke-cancel') || {}).version || 1 }, REINSTATE_JOB: { job_id: 'J-s17-smoke-cancel', version: 'AFTER_CANCEL' } } }; }); }); }
