const fs = require('node:fs'), schema = require('../schema/tables.json');
const names = ['ScaffoldBookings', 'Companies', 'Contacts', 'Jobs', 'Tasks', 'TaskTemplates', 'People', 'Issues', 'Communications', 'CommunicationJobs', 'Acknowledgements', 'Settings', 'Holidays', 'CommitJournal', 'AuditEvents', 'ReleaseModes'];
const headers = Object.fromEntries(names.map(n => [n, schema.tables.find(t => t.name === n).columns.map(c => c.name)]));
const files = ['scaffold/workflow.js', 'scaffold/cloud-adapter.js'];
const text = '/* Generated by npm run build:scaffold. DEV scaffold workflow; captured communications only, no sends. */\nvar SCF_HEADERS=' + JSON.stringify(headers) + ';\n' +
  files.map(f => fs.readFileSync(f, 'utf8').replace(/^'use strict';\n/m, '').replace(/^if \(typeof module !== 'undefined'\) \{[\s\S]*?\n\}\n?/m, '')).join('\n');
fs.mkdirSync('apps-script/scaffold', { recursive: true });
fs.writeFileSync('apps-script/scaffold/ScaffoldWorkflow.js', text);
