const fs = require('node:fs'), schema = require('../schema/tables.json');
const names = ['Materials', 'Orders', 'OrderLines', 'Deliveries', 'ReceiptLines', 'StockMovements', 'StockLocations', 'Products', 'Companies', 'Contacts', 'Customers', 'Jobs', 'WorkPackages', 'Tasks', 'TaskTemplates', 'People', 'Issues', 'Evidence', 'Communications', 'CommunicationJobs', 'Acknowledgements', 'Settings', 'Holidays', 'CommitJournal', 'AuditEvents', 'ReleaseModes'];
const headers = Object.fromEntries(names.map(n => [n, schema.tables.find(t => t.name === n).columns.map(c => c.name)]));
const files = ['materials/workflow.js', 'materials/cloud-adapter.js'];
const text = '/* Generated by npm run build:materials. DEV materials workflow; captured merchant messages only, no sends. */\nvar MAT_HEADERS=' + JSON.stringify(headers) + ';\n' +
  files.map(f => fs.readFileSync(f, 'utf8').replace(/^'use strict';\n/m, '').replace(/^if \(typeof module !== 'undefined'\) \{[\s\S]*?\n\}\n?/m, '')).join('\n');
fs.mkdirSync('apps-script/materials', { recursive: true });
fs.writeFileSync('apps-script/materials/MaterialsWorkflow.js', text);
