const fs = require('node:fs'), schema = require('../schema/tables.json');
const names = ['People', 'PersonRoles', 'PersonSkills', 'PersonAvailability', 'Teams', 'TeamMembers', 'Jobs', 'WorkPackages', 'Allocations', 'CalendarLinks', 'Materials', 'OrderLines', 'Orders', 'ScaffoldBookings', 'Settings', 'Holidays', 'CommitJournal', 'AuditEvents'];
const headers = Object.fromEntries(names.map(n => [n, schema.tables.find(t => t.name === n).columns.map(c => c.name)]));
const files = ['resource/planning.js', 'resource/cloud-adapter.js'];
const text = '/* Generated by npm run build:resource. DEV resource planning (skills, leave, teams, assessment). No external calls. */\nvar RP_HEADERS=' + JSON.stringify(headers) + ';\n' +
  files.map(f => fs.readFileSync(f, 'utf8').replace(/^'use strict';\n/m, '').replace(/^if \(typeof module !== 'undefined'\) \{[\s\S]*?\n\}\n?/m, '')).join('\n');
fs.mkdirSync('apps-script/resource', { recursive: true });
fs.writeFileSync('apps-script/resource/ResourcePlanning.js', text);
