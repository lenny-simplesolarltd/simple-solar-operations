{
  "name": "simple-solar-s01",
  "version": "0.1.0",
  "private": true,
  "description": "S01 environment safeguards only",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22"
  }
}
