# S01 fixture and reset

`s01-probes.json` is synthetic local test input, not customer data or a real recipient configuration. `example.invalid` addresses never count as delivered inboxes. Local tests start fresh objects and stub services on every test; rerun `npm test` to reset/load. They create no remote records and do not read private configuration.

Full business fixture F0 and its loader belong to S02, not this step. When implemented, preserve document 03 §1 exactly: clock Friday 23 October 2026 09:00 Europe/London; Monday–Friday; no holidays in that synthetic base; named test roles; distinct A/B customer identities despite same display label; P460/P515 and opening balances; contract/invoice states. Do not seed these business tables in S01. Reset each independent acceptance case to F0; preserve only explicitly chained stock cases. Never use real customers or guess missing values.

For manual S01 probes: record release/config/project/calendar IDs in protected evidence first. Set CAPTURE/disabled after each session. Retain execution logs, receipt screenshots and event IDs before cleanup. A timeout may have sent already: inspect the labelled test inbox/calendar and reconcile before any rerun. No automatic retry is provided. Clean up only identified synthetic probe events in the independently verified TEST calendar, with test recipients; cleanup can itself send cancellation notices. Never bulk clear a calendar/folder or touch PROD. The company owner decides retention for evidence. Future reset tools must fail closed on PROD, unknown/mismatched IDs or mixed resources and must disable outbound actions during restore/reset.
