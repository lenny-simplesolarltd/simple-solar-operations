# Existing automation discovery

`inventory.csv` contains discovery placeholders, not an audited list of live integrations. The two form URLs come from specification 01 §14; neither was opened/submitted. Copy to company-restricted storage, add one row per actual Zap/form/calendar/invoice/message/action/Sheet/project, and complete every unknown with the current owner. Capture inputs/outputs, exact trigger and identifiers, account/credential owner (never the credential), current live state, proposed future owner, replacement stage, retain/retire decision, cutover and rollback.

Ben owns access/priorities and legacy retain/retire decisions. Tanya helps reconcile operational processes. Existing connection owners export current settings read-only. Record export time/version/checksum and company evidence reference. Preserve originals in protected company storage; redact before sharing or committing. Raw exports can contain credentials and customer records: use `integration-inventory/private/` only for restricted local working files, never commit them. Public repository rows must use protected references for production identifiers.

For Jotform obtain current question-ID export later and have Ben/Tanya approve the mapping at S05. Do not infer question IDs from labels or older workbooks. Do not submit a live form for discovery. Record each downstream producer, including ones hidden behind the forms. Inventory technical/account limitations in the entitlement register. No legacy Zap, trigger, sheet, scaffold workflow, invoice action or other producer is disabled in S01. RA01 repeats S19/S20 for each release; each scoped transfer must reconcile IDs and establish one producer per outgoing action; rollback must preserve already-created external IDs and prevent replay.

## Confirmed operational baseline

[DEC-001](../docs/implementation-decisions.md) records Ben / Simple Solar's confirmation that materials/scaffolding, installer processes and finance/reporting already operate in some form. Discovery must identify those existing processes and their producers rather than assume they are absent. RA01/DEC-002 supersedes DEC-001’s order: default R1 Office → R2 Materials/scaffolding → R3 Installer commissioning → R4 Finance/reporting. The recognition of existing processes remains valid. This order does not establish individual integration live states or assign replacement dates. Complete inventory, reconciliation, tests and the specification's safe cutover/rollback steps before replacing any process.

## RA01 release ownership and mode register

`release-ownership-modes.csv` is a planning template, not live configuration. `mode` uses only Disabled / Manual / Automated and records a proposed new-system baseline; existing live producers are not being disabled. `planned_R1_mode` specifies the later proposed R1 route, conditional on approval. Actual existing systems, connection owners, IDs and access still need verification. GHL remains Manual in every release.

Before activation, copy to protected company storage and create one row per function/producer/authorised job-package-location scope. Record current system, responsible person, external IDs, activation timestamp, approved version, Ben decision and fallback. Separate pilot and non-pilot routes; current automated Zaps can remain behind a new-system Manual task. Preserve prior register versions/audit and explicit job/package/form-version capture boundaries. No identifiers, approval times or production credentials may be guessed or committed here.

Future processor enforcement must reject disabled/out-of-scope actions and duplicate source events; hiding buttons is insufficient. Reconcile old IDs/state/open tasks before takeover, complete with evidence or supersede by linked replacement. If producer exclusion is unreliable, keep manual or use a tested whole-function cutover; never two creators for the same scope. R2 stock must cover every movement at its locations including non-pilot jobs or the new balance stays Disabled. Run RT01–RT08 as allocated in the test plan. No mode enforcement or transition is implemented in S01 documentation work.
